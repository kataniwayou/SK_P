---
task: 260614-9jd-remove-dead-skp-dlq-1-dead-letter-tier-c
type: quick-full
mode: quick-full
autonomous: true
files_modified:
  - src/BaseConsole.Core/Messaging/ConsolidatedErrorTransportFilter.cs   # DELETE
  - src/BaseConsole.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs
  - tests/BaseApi.Tests/Keeper/KeeperDlqConsolidationTests.cs            # DELETE
  - tests/BaseApi.Tests/Keeper/RecoveryDeadLetterFacts.cs
  - tests/BaseApi.Tests/Orchestrator/SC2RecoveryPathsE2ETests.cs
  - scripts/phase-62-close.ps1

must_haves:
  truths:
    - "Nothing in src/ references ConsolidatedFault, ConsolidatedErrorTransportFilter, or the Dlq1 const — the dead tier's production surface is gone."
    - "The skp-dlq-1 topology (queue decl w/ x-message-ttl, BindQueue, Publish<ConsolidatedFault> hook) is removed from BaseConsole.Core messaging wiring."
    - "SK_P.sln builds -c Debug AND -c Release with exit 0 and zero warnings (no orphaned usings, no dangling type refs) across all projects."
    - "The hermetic Keeper + Resilience suites stay green: the consolidated-DLQ proof test is gone, the recovery dead-letter fact is reduced to a fault/nack-requeue shape with no DLQ-type reference, and the source-scan facts still hold."
    - "scripts/phase-62-close.ps1 no longer probes skp-dlq-1 depth==0; the triple-SHA, the skp:msg:* count==0 assertion, and all other gate logic are intact."
    - "phase-39/49/55/58-close.ps1 are untouched."
  artifacts:
    - path: "src/BaseConsole.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs"
      provides: "Bus skeleton with the dead skp-dlq-1 topology block removed; correlation filters + configureBus seam intact"
      contains: "AddBaseConsoleMessaging"
    - path: "tests/BaseApi.Tests/Keeper/RecoveryDeadLetterFacts.cs"
      provides: "Reduced keeper-recovery fault/nack-requeue fact with NO ConsolidatedFault / Dlq1 type reference"
      contains: "InfraFault_reinject_faults"
    - path: "scripts/phase-62-close.ps1"
      provides: "Phase 62 close gate without the skp-dlq-1 depth probe; triple-SHA + skp:msg:* assertions preserved"
      contains: "skp:msg:* count invariant"
  key_links:
    - from: "grep -r over src/"
      to: "zero hits for ConsolidatedFault|ConsolidatedErrorTransportFilter|\\bDlq1\\b"
      via: "ref-sweep assertion after the production delete"
    - from: "MessagingServiceCollectionExtensions.cs (using BaseConsole.Core.Messaging)"
      to: "InboundCorrelationConsumeFilter / Outbound*Filter (same namespace, STILL referenced)"
      via: "the using is RETAINED — only the ConsolidatedFault refs are stripped (NOT an orphaned using)"
---

<objective>
Delete the dead `skp-dlq-1` consolidated dead-letter tier. After quick task 260614-2hf made the keeper-recovery endpoint nack-requeue with no `ConfigureError`, the keeper's binder was the SOLE remaining producer into `skp-dlq-1`. The processor-dispatch and orchestrator-result exec-path endpoints never had an error pipeline. So the `skp-dlq-1` queue, the `ConsolidatedErrorTransportFilter`, and the `ConsolidatedFault` envelope type are all orphaned dead code.

This is a clean DELETION (verified: no `ConfigureError`/`GenerateFaultFilter` producer call survives anywhere in `src/` outside the file being deleted). Remove the production filter + topology, fix/delete the affected tests, and strip ONLY the `skp-dlq-1` probe from the live phase-62 close script.

Purpose: eliminate dead code + a dead broker topology so the 0-warning gate and the close-gate net-zero surface carry no orphaned tier.
Output: deleted filter file + deleted consolidation test, slimmed messaging wiring, reduced recovery fact, compile-fixed SC2 E2E, slimmed phase-62 close script.
</objective>

<context>
@.planning/STATE.md

# Production surface being deleted (read in planning):
#   src/BaseConsole.Core/Messaging/ConsolidatedErrorTransportFilter.cs  — the filter + the ConsolidatedFault record (whole file → DELETE)
#   src/BaseConsole.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs — the skp-dlq-1 topology block

<interfaces>
<!-- Verified facts the executor needs — no codebase re-exploration required. -->

1. SOLE producer confirmation: grep of src/ for ConfigureError|GenerateFaultFilter returns ONLY
   ConsolidatedErrorTransportFilter.cs + MessagingServiceCollectionExtensions.cs (the files being changed)
   + doc-comment-only mentions in RecoveryConsumerBase.cs / RecoveryEndpointBinder.cs. There is genuinely
   NO remaining producer into skp-dlq-1 post-260614-2hf.

2. RETAINED using: MessagingServiceCollectionExtensions.cs line 2 `using BaseConsole.Core.Messaging;` is
   STILL needed — the four correlation filters (InboundCorrelationConsumeFilter, InboundExecutionScopeConsumeFilter,
   OutboundCorrelationSendFilter, OutboundCorrelationPublishFilter) live in that same namespace and are still
   referenced. DO NOT remove that using. Only strip the ConsolidatedFault / ConsolidatedErrorTransportFilter
   refs inside the method body.

3. The block to delete in MessagingServiceCollectionExtensions.cs is the contiguous span ~lines 49-98:
   - the A18/Phase-53 comment paragraph (lines 49-53) that names the removed move + says the topology is "KEPT"
   - the entire DLQ-1 topology comment (lines 63-87)
   - `c.DeployPublishTopology = true;` (line 88)  ← this exists ONLY for the ConsolidatedFault eager-deploy; remove it
   - the whole `c.Publish<ConsolidatedFault>(p => { ... p.BindQueue(...x-message-ttl...); });` block (lines 89-98)
   Keep: the four UseConsumeFilter/UseSendFilter/UsePublishFilter lines, configureBus?.Invoke, c.ConfigureEndpoints(ctx).

4. COMPILE HAZARD (RealStack E2E, but breaks the BUILD): SC2RecoveryPathsE2ETests.cs has
   `using BaseConsole.Core.Messaging;` (line 9) and 3 hard type refs to `ConsolidatedErrorTransportFilter.Dlq1`
   (lines 148, 165, 168, 175). These WILL fail to compile once the type is gone. Replace each
   `ConsolidatedErrorTransportFilter.Dlq1` with the literal "skp-dlq-1" and drop the now-orphaned
   `using BaseConsole.Core.Messaging;` (verify no other member of that namespace is used in the file —
   grep showed only the Dlq1 refs). The STATE-2 data-gone block logic (assert DLQ depth does NOT climb —
   it's a by-design drop) is preserved verbatim, just with the literal queue name.

5. SOURCE-SCAN guard (ModelBContractsRetiredFacts.cs, Resilience namespace — hermetic): FACT 7 asserts
   `Assert.DoesNotContain("ConfigureError", MessagingServiceCollectionExtensions.cs)`. The comment being
   deleted says "...GenerateFaultFilter + ConsolidatedErrorTransportFilter → skp-dlq-1) is REMOVED" — it does
   NOT contain the literal substring "ConfigureError", so FACT 7 stays GREEN whether or not you delete that
   comment. FACT 6 scans src/Keeper/Recovery for the CALL pattern ".ConfigureError(" (with paren) — the
   doc-comment mentions there have no paren-call, so they stay GREEN too. No change needed to either fact, and
   the RecoveryConsumerBase.cs / RecoveryEndpointBinder.cs doc comments may be left as-is (no source-scan asserts
   on the word "skp-dlq-1" in those files).

6. OUT-OF-GATE files that COMPILE FINE (string literals / comments only) — LEAVE per scope_boundary:
   - RealStackNetZeroSweepFixture.cs: uses literal "skp-dlq-1" in a best-effort try/catch purge_queue — compiles,
     functionally harmless (purging a now-absent queue is a no-op swallowed by catch). LEAVE.
   - SC3PauseResumeOutageE2ETests.cs, KeeperPauseAccumulateFacts.cs, CollectionDefinitions.cs: comment-only. LEAVE.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the production filter + topology, sweep all src/ refs (0-warning gate)</name>
  <files>
    src/BaseConsole.Core/Messaging/ConsolidatedErrorTransportFilter.cs (DELETE),
    src/BaseConsole.Core/DependencyInjection/MessagingServiceCollectionExtensions.cs
  </files>
  <action>
1. DELETE the whole file `src/BaseConsole.Core/Messaging/ConsolidatedErrorTransportFilter.cs` (it holds both the `ConsolidatedErrorTransportFilter` filter and the `ConsolidatedFault` record). Use the OS delete (e.g. `Remove-Item`), not a Write/empty.

2. In `MessagingServiceCollectionExtensions.cs`, remove the dead skp-dlq-1 topology per interfaces note #3:
   - delete the A18/Phase-53 comment paragraph that names the removed move and claims the topology is "KEPT"
   - delete the entire `// DLQ-1 (D-05/07 ...)` topology comment block
   - delete `c.DeployPublishTopology = true;` (it exists ONLY for the ConsolidatedFault eager deploy)
   - delete the whole `c.Publish<ConsolidatedFault>(p => { ... p.BindQueue(ConsolidatedErrorTransportFilter.Dlq1, ..., q => q.SetQueueArgument("x-message-ttl", ...)); });` block
   KEEP the four correlation filter registrations, `configureBus?.Invoke(ctx, c);`, and `c.ConfigureEndpoints(ctx);`.
   KEEP `using BaseConsole.Core.Messaging;` (line 2) — the correlation filters in that namespace are still referenced (interfaces note #2). Do NOT remove any other using.

3. REF SWEEP across ALL projects: grep src/ for `ConsolidatedFault`, `ConsolidatedErrorTransportFilter`, `\bDlq1\b`, `Dlq1Uri`. The ONLY remaining hits permitted in src/ are doc-comment mentions of the bare string "skp-dlq-1" in `RecoveryConsumerBase.cs` and `RecoveryEndpointBinder.cs` (no source-scan test asserts on them — leave as-is). There must be ZERO remaining hits for `ConsolidatedFault` / `ConsolidatedErrorTransportFilter` / `Dlq1` / `Dlq1Uri` anywhere under src/. Fix any orphaned `using` lines a removal exposes (watch BaseConsole.Core, Keeper, Orchestrator, BaseProcessor.Core).
  </action>
  <verify>
    <automated>dotnet build SK_P.sln -c Debug --nologo; if ($LASTEXITCODE -ne 0) { exit 1 }; dotnet build SK_P.sln -c Release --nologo; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>ConsolidatedErrorTransportFilter.cs is gone; the skp-dlq-1 Publish/BindQueue/DeployPublishTopology block is removed from MessagingServiceCollectionExtensions.cs; `grep -rE "ConsolidatedFault|ConsolidatedErrorTransportFilter|\bDlq1\b" src/` returns no hits; SK_P.sln builds clean (exit 0, 0 warnings) in BOTH Debug and Release.</done>
</task>

<task type="auto">
  <name>Task 2: Fix/delete affected tests + slim the phase-62 close script</name>
  <files>
    tests/BaseApi.Tests/Keeper/KeeperDlqConsolidationTests.cs (DELETE),
    tests/BaseApi.Tests/Keeper/RecoveryDeadLetterFacts.cs,
    tests/BaseApi.Tests/Orchestrator/SC2RecoveryPathsE2ETests.cs,
    scripts/phase-62-close.ps1
  </files>
  <action>
1. DELETE `tests/BaseApi.Tests/Keeper/KeeperDlqConsolidationTests.cs` whole — it is the proof of the removed consolidated-DLQ behavior (every fact references the deleted type).

2. `tests/BaseApi.Tests/Keeper/RecoveryDeadLetterFacts.cs` — reduce to a fault/nack-requeue shape with NO DLQ-type reference:
   - remove `using BaseConsole.Core.Messaging;` (line 1)
   - in `BuildHarness`, delete the consolidated-sink handler declaration (the `x.AddHandler((ConsumeContext<ConsolidatedFault> _) => ...).Endpoint(e => e.Name = ConsolidatedErrorTransportFilter.Dlq1);` block, lines 62-65)
   - in `InfraFault_reinject_faults_and_does_not_dead_letter`, delete the negative ConsolidatedFault assertion (the `using var window ... Assert.False(await harness.Consumed.Any<ConsolidatedFault>(...))` block, lines 100-104). Keep the positive fault assertion `Assert.True(await harness.Consumed.Any<KeeperReinject>(f => f.Exception is not null, ct));` — that IS the fault/nack-requeue shape (the bare endpoint has no error transport, so a faulted consume falls through to broker nack-requeue).
   - reword the class/method XML doc so it no longer claims a ConsolidatedFault sink is declared (it asserts: infra-fault reinject FAULTS through the Guard on exhaustion, the faulted consume is not acked → broker nack-requeue, NO dead-letter tier exists).
   - LEAVE the `Duplicate_Reinject_reproduces_effect_no_collapse` fact (RESIL-03) and `PresentMux` ALONE — they have no DLQ reference.

3. `tests/BaseApi.Tests/Orchestrator/SC2RecoveryPathsE2ETests.cs` — COMPILE fix only (interfaces note #4):
   - remove `using BaseConsole.Core.Messaging;` (line 9) — verify no other member of that namespace is used (grep confirmed only the Dlq1 refs).
   - replace each `ConsolidatedErrorTransportFilter.Dlq1` (lines 148, 165, 168, 175) with the literal `"skp-dlq-1"`. Do NOT change the surrounding STATE-2 data-gone logic — it asserts the DLQ depth does NOT climb (by-design silent drop), which stays valid; the defensive `BrokerQueuesToPurge.Add("skp-dlq-1")` stays as a self-healing no-op.

4. `scripts/phase-62-close.ps1` — remove ONLY the skp-dlq-1 probe (interfaces / scope_boundary):
   - delete the `// ---- Single-DLQ depth==0 assertion ----` comment block + the entire `foreach ($q in @('skp-dlq-1')) { ... }` depth loop (lines ~449-475).
   - delete the `Write-Host "  DLQ depth: skp-dlq-1=0" -ForegroundColor Green` summary line (line ~499).
   - in the final operator-append `Write-Host` (line ~501), drop the `+ the skp-dlq-1 depth` token, keeping the three SHA values + Passed count + skp:msg:* count.
   - OPTIONAL housekeeping (does not affect gate behavior): the header comment lines that describe a "separate DLQ depth==0" / "single DLQ skp-dlq-1" may be trimmed for accuracy, but this is cosmetic — the load-bearing change is removing the runtime probe. KEEP everything else: triple-SHA (psql/redis/rabbitmq), the `skp:msg:*` count==0 assertion, the build gate, the 3-GREEN cadence, exit codes. DO NOT touch phase-39/49/55/58-close.ps1.
  </action>
  <verify>
    <automated>dotnet build tests/BaseApi.Tests/BaseApi.Tests.csproj -c Debug --nologo; if ($LASTEXITCODE -ne 0) { exit 1 }; tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-namespace "BaseApi.Tests.Keeper"; if ($LASTEXITCODE -ne 0) { exit 1 }; tests/BaseApi.Tests/bin/Debug/net8.0/BaseApi.Tests.exe --filter-namespace "BaseApi.Tests.Resilience"</automated>
  </verify>
  <done>KeeperDlqConsolidationTests.cs is deleted; RecoveryDeadLetterFacts.cs has no ConsolidatedFault/Dlq1 reference and its remaining facts pass; SC2RecoveryPathsE2ETests.cs compiles with "skp-dlq-1" literals and no BaseConsole.Core.Messaging using; phase-62-close.ps1 has no skp-dlq-1 probe but keeps the triple-SHA + skp:msg:* count==0; phase-39/49/55/58-close.ps1 are byte-unchanged; the hermetic Keeper + Resilience suites are green (Redis localhost:6380 up; RealStack/E2E excluded — RabbitMQ may be down — expected).</done>
</task>

</tasks>

<verification>
- Build gate (overall): `dotnet build SK_P.sln -c Debug --nologo` AND `-c Release --nologo` → exit 0, 0 warnings. Watch orphaned usings / dangling type refs across BaseConsole.Core, Keeper, Orchestrator, BaseApi.Service, BaseProcessor.Core.
- Ref-sweep assertion: `grep -rE "ConsolidatedFault|ConsolidatedErrorTransportFilter|\bDlq1\b|Dlq1Uri" src/` → no hits (only bare-string "skp-dlq-1" doc comments in RecoveryConsumerBase.cs / RecoveryEndpointBinder.cs are permitted to survive).
- Hermetic suites (Redis localhost:6380 up; RabbitMQ may be down → RealStack/E2E excluded, expected):
  `BaseApi.Tests.exe --filter-namespace "BaseApi.Tests.Keeper"` → green
  `BaseApi.Tests.exe --filter-namespace "BaseApi.Tests.Resilience"` → green (FACT 6 + FACT 7 source-scans still hold)
- Frozen-archive guard: `git diff --stat` shows NO change to scripts/phase-39-close.ps1, phase-49-close.ps1, phase-55-close.ps1, phase-58-close.ps1.
</verification>

<success_criteria>
- `ConsolidatedErrorTransportFilter.cs` deleted; `ConsolidatedFault` + `ConsolidatedErrorTransportFilter` + `Dlq1`/`Dlq1Uri` have zero references anywhere under src/.
- skp-dlq-1 topology (queue decl w/ x-message-ttl=604800000, BindQueue, Publish<ConsolidatedFault>, DeployPublishTopology) removed from MessagingServiceCollectionExtensions.cs; the four correlation filters + configureBus seam intact; `using BaseConsole.Core.Messaging;` retained (still needed).
- SK_P.sln builds clean (exit 0, 0 warnings) in Debug AND Release.
- KeeperDlqConsolidationTests.cs deleted; RecoveryDeadLetterFacts.cs reduced to a fault/nack-requeue shape with no DLQ-type reference; SC2RecoveryPathsE2ETests.cs compiles via "skp-dlq-1" literals; hermetic Keeper + Resilience suites green.
- phase-62-close.ps1: skp-dlq-1 depth probe + its summary line + operator-append token removed; triple-SHA + skp:msg:* count==0 + build gate + 3-GREEN cadence preserved. phase-39/49/55/58-close.ps1 untouched.
</success_criteria>

<output>
After completion, create `.planning/quick/260614-9jd-remove-dead-skp-dlq-1-dead-letter-tier-c/260614-9jd-SUMMARY.md` recording: the deleted files, the topology lines removed, the test reductions, the close-script probe removal, the ref-sweep result, and the build/hermetic-suite outcomes.
</output>
