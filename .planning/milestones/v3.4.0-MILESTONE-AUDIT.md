---
milestone: v3.4.0
milestone_name: BaseConsole + Orchestrator Messaging
audited: 2026-05-31T01:00:00Z
status: tech_debt
scores:
  requirements: 37/37 satisfied (34 via VERIFICATION.md, 3 of those discharged cross-phase; 7 via close-gate+integration+SUMMARY — Phase 20 verification-doc gap)
  phases: 4/4 complete
  integration: 6/6 seams wired (PASS)
  flows: 1/1 E2E correlation flow proven (CORR-04 / TEST-RMQ-02 GREEN on live stack)
gaps:
  requirements: []   # none unsatisfied — FAIL gate not triggered
  integration: []    # 0 BLOCKERs
  flows: []
process_gaps:        # non-blocking, but real
  - id: "PHASE-20-NO-VERIFICATION"
    detail: "Phase 20 has no 20-VERIFICATION.md (only 20-VALIDATION.md). Its 7 requirements (CORR-03/04, TEST-RMQ-01..05) are evidenced by SUMMARY frontmatter + the close gate (3x265 GREEN + triple-SHA exit 0) + the integration check, but were never run through gsd-verifier. Per the audit status matrix this is 'partial (verification gap)', not 'unsatisfied'."
  - id: "TRACEABILITY-STALE"
    detail: "REQUIREMENTS.md traceability table has 13 stale rows: the 12 Phase-19 rows (ORCH-CON-01..04, MSG-WEBAPI-01..04, MSG-ACK-01..04) read 'Pending' despite [x] checkboxes + VERIFICATION SATISFIED; TEST-RMQ-05 reads 'Pending' + [ ] despite being Done (gate exit 0). Doc-tracking lag, not a code gap."
  - id: "PHASE-19-HUMAN-UAT-DISCHARGED"
    detail: "19-VERIFICATION status is human_needed (1 item: live cross-process correlation). That item was explicitly designed to be absorbed + automated by Phase 20 TEST-RMQ-02/CORR-04, which is now GREEN on the live stack. The human-UAT is therefore discharged by automation; 19-HUMAN-UAT.md still reads status: partial / pending."
tech_debt:
  - phase: 17-messaging-contracts
    items:
      - "17-VALIDATION.md left in draft (nyquist_compliant: false) — same posture as the v3.3.0-deferred Phases 12/14/16 (passed VERIFICATION + close gate). /gsd-validate-phase 17 to close."
  - phase: 18-baseconsole-core
    items:
      - "[CORRECTED 2026-05-31] WR-01 and WR-02 were ALREADY FIXED in Phase 18 — this audit mis-flagged them by carrying them forward from 18-VERIFICATION.md's anti-pattern table, which predated the same-phase follow-up commits. WR-01 (StopAsync disposes inner WebApplication) fixed in d4c0af5; WR-02 (bind-failure try/catch with actionable port log) fixed in 4e9e21a. Verified in live source. NOT open tech debt. Only WARNING-1 (Phase 19, below) remains → Phase 21 / HARDEN-03."
  - phase: 19-orchestrator-webapi-bus
    items:
      - "Cross-phase WARNING-1: OrchestratorL2Keys.Root is a hand-copy of RedisProjectionKeys.Root (writer is internal). Byte-identical today; a future GUID-format/suffix change to the writer would silently break the reader with no compile/unit-test failure (only the E2E couples them). Canonical fix: hoist Root into Messaging.Contracts. Consciously deferred (19-RESEARCH Open Q3)."
  - phase: 20-correlation-proof-closeout
    items:
      - "WARNING-2 (doc nit): CorrelationPropagationE2ETests.cs:31 comment says the Start path writes 'skp:wf:{id}:root'; actual shape is 'skp:{wfId}' (matches cleanup). Stale prose, wiring correct."
nyquist:
  compliant_phases: [18, 19, 20]
  partial_phases: [17]
  missing_phases: []
  overall: "3/4 compliant; Phase 17 VALIDATION still draft (nyquist_compliant: false)"
---

# Milestone v3.4.0 — Audit Report (BaseConsole + Orchestrator Messaging)

**Audited:** 2026-05-31 · **Status:** `tech_debt` (no blockers; accumulated review items) · **Scope:** Phases 17–20

## Verdict

The milestone **achieved its definition of done**: a reusable `BaseConsole.Core` Generic-Host library and a runnable `Orchestrator` console, wired to the WebApi over MassTransit/RabbitMQ, with **per-stage CorrelationId observability proven end-to-end in Elasticsearch** (HTTP stage id → fresh publish-minted body Guid → orchestrator log scope), stopping cleanly at the scheduler-job-start seam. The triple-SHA close gate passes at **exit 0 (3× consecutive 265-GREEN, psql + redis + rabbitmq BEFORE==AFTER all held)**.

No requirement is `unsatisfied` → the FAIL gate is not triggered. Status is `tech_debt` rather than a clean `passed` because of one real **verification-process gap** (Phase 20 has no VERIFICATION.md) plus traceability-doc staleness and a handful of deferred code-hygiene items — all non-blocking, all worth a glance before archive.

## Requirements Coverage (3-source cross-reference: VERIFICATION + SUMMARY frontmatter + traceability)

| Phase | Reqs | VERIFICATION.md | SUMMARY frontmatter | Traceability | Final |
|-------|------|-----------------|---------------------|--------------|-------|
| 17 | MSG-CONTRACTS-01..04, INFRA-RMQ-01 (5) | **passed** (9/9 truths, all 5 SATISFIED w/ code evidence) | only MSG-CONTRACTS-04 listed (17-01 has no frontmatter) | 4× Pending, 1× Complete (stale) | **satisfied** (VERIFICATION is authoritative; frontmatter + table lag) |
| 18 | CONSOLE-01..05, CONSOLE-HEALTH-01..04, CORR-01/02 (11) | **passed** (5/5 truths, 11/11 SATISFIED) | all 11 listed (18-04) | all Complete | **satisfied** |
| 19 | ORCH-CON-01..04, MSG-WEBAPI-01..04, MSG-ACK-01..04, INFRA-RMQ-02/03 (14) | **human_needed** (5/5 truths verified statically; 1 human item) | MSG-WEBAPI-01..04 + INFRA-RMQ-02/03 listed; ORCH-CON/MSG-ACK not in frontmatter | 12× Pending (stale), 2× Complete | **satisfied** — human item discharged by Phase 20 TEST-RMQ-02 (now GREEN live) |
| 20 | CORR-03/04, TEST-RMQ-01..05 (7) | **MISSING** (only VALIDATION.md) | all 7 listed across 20-01..04 | 6× Done, TEST-RMQ-05 Pending (stale) | **satisfied (verification-doc gap)** — close gate exit 0 + integration PASS are the evidence |

**37/37 requirements satisfied by evidence. 0 orphaned** (every REQ-ID maps to exactly one phase and appears in that phase's VERIFICATION or — for Phase 20 — the close-gate + integration check + SUMMARY frontmatter).

## Cross-Phase Integration — PASS (gsd-integration-checker)

6/6 seams verified wired at source level, 0 BLOCKERs, 0 broken flows, 0 dependency-direction violations:

1. **Publisher↔contract↔consumer** — both bind the same `Messaging.Contracts` types; correlation key is one shared constant (`CorrelationKeys.LogScope="CorrelationId"`). OK
2. **Per-stage correlation handoff** — publish mints fresh body Guid (`NewId.NextGuid()`); inbound filter reads body via `as ICorrelated`; E2E asserts `NotEqual(httpCorr, bodyGuid)` + same-Guid term-match across both ES docs. OK
3. **Dependency firewall** — no `BaseConsole.Core → BaseApi.Core`; WebApi never references BaseConsole.Core; both reference only Messaging.Contracts (verified via .csproj). OK
4. **RabbitMQ soft-dep asymmetry** — WebApi `MinimalFailureStatus=Degraded` keeps CRUD `/health/ready` 200 when broker down; Orchestrator `/health/ready` → Unhealthy. OK
5. **Redis L2 key byte-shape** — writer `{prefix}{wf}` and reader `{prefix}{wf:D}` render identically; prefix `skp:` agrees. OK (see WARNING-1)
6. **Compose tier** — rabbitmq 4.1.8 (5673), redis (6380), postgres (5433), otel (4317); both services depends_on service_healthy; ports match test fixtures. OK

WARNINGs (both non-blocking, captured in tech_debt): WARNING-1 duplicated L2 key shape across the phase boundary; WARNING-2 stale comment in the E2E test.

## Nyquist Coverage

| Phase | VALIDATION.md | nyquist_compliant | Action |
|-------|---------------|-------------------|--------|
| 17 | exists (draft) | **false** | `/gsd-validate-phase 17` (optional — matches v3.3.0-deferred posture) |
| 18 | exists (validated) | true | — |
| 19 | exists (draft) | true | — |
| 20 | exists | true | — |

## What's Strong

- The milestone's own DoD instrument — the triple-SHA close gate — **passes at exit 0** with full-stack 3×265 GREEN. This is the highest-confidence evidence available and covers the entire suite, not just Phase 20.
- The headline deliverable (per-stage correlation proven in Elasticsearch) is verified by a live cross-process E2E (`CorrelationPropagationE2ETests`), which also retires the only Phase 19 human-UAT item.
- Integration check found zero wiring breaks across all four phases.

## What Needs a Glance Before Archive (none blocking)

1. **Phase 20 verification-doc gap** — no `20-VERIFICATION.md`. The evidence exists (gate + integration + SUMMARYs); only the formal verifier pass is absent. Optional: `/gsd-verify-work 20` to produce it.
2. **Traceability staleness** — 13 rows in REQUIREMENTS.md lag reality (12 Phase-19 "Pending", TEST-RMQ-05 "Pending"/unchecked). Pure doc hygiene; `/gsd-complete-milestone` typically reconciles these.
3. **Phase 19 human-UAT doc** — `19-HUMAN-UAT.md` still reads `partial/pending` though the item is discharged by TEST-RMQ-02.
4. **Code hygiene** — WR-01/WR-02 (console health-listener dispose + bind isolation), WARNING-1 (L2 key duplication), Phase 17 nyquist draft.

---
*Audited: 2026-05-31 · gsd-audit-milestone · integration check via gsd-integration-checker*
