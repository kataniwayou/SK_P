---
milestone: v3.4.0
milestone_name: BaseConsole + Orchestrator Messaging
audited: 2026-05-31T12:00:00Z
audit_revision: 2   # re-audit after Phase 21 (HARDEN-03) closeout; supersedes rev 1 (2026-05-31T01:00:00Z, pre-Phase-21)
status: tech_debt
scope: Phases 17-21
scores:
  requirements: 40/40 satisfied (33 via VERIFICATION.md SATISFIED tables; 7 Phase-20 via close-gate+integration+SUMMARY — verification-doc gap)
  phases: 5/5 complete
  integration: 6/6 seams wired (PASS, 0 blockers, 0 warnings)
  flows: 1/1 E2E correlation flow proven (CORR-04 / TEST-RMQ-02 GREEN on live stack)
gaps:
  requirements: []   # none unsatisfied — FAIL gate NOT triggered
  integration: []    # 0 BLOCKERs, 0 WARNINGs (WARNING-1 closed by Phase 21)
  flows: []
closed_since_rev1:
  - id: "WARNING-1 / HARDEN-03"
    detail: "The duplicated L2 root key shape (OrchestratorL2Keys.Root hand-copied RedisProjectionKeys.Root) — the one genuinely-open code item from rev 1 — is CLOSED by Phase 21. Both writer and reader now delegate to the shared Messaging.Contracts.Projections.L2ProjectionKeys; zero surviving hand-copied interpolation literals (integration check re-verified PASS). 21-VERIFICATION passed 6/6; triple-SHA gate exit 0 at 270x3 GREEN incl. CorrelationPropagationE2ETests."
  - id: "WR-01 / WR-02"
    detail: "Confirmed (rev 1 corrected) already fixed in Phase 18 (commits d4c0af5, 4e9e21a). HARDEN-01/HARDEN-02 track them as Complete. Not open."
process_gaps:        # non-blocking, but real — worth a glance before archive
  - id: "PHASE-20-NO-VERIFICATION"
    detail: "Phase 20 still has no 20-VERIFICATION.md (only 20-VALIDATION.md, which is the pre-execution 'planned' contract). Its 7 requirements (CORR-03/04, TEST-RMQ-01..05) are evidenced by SUMMARY frontmatter + the close gate (3x265 GREEN + triple-SHA exit 0) + the integration check (PASS) + Phase 21's full-suite 270x3 GREEN run that INCLUDES CorrelationPropagationE2ETests. Per the audit status matrix this is 'partial (verification gap)', NOT 'unsatisfied'. Optional remediation: /gsd-verify-work 20."
  - id: "TRACEABILITY-STALE"
    detail: "REQUIREMENTS.md traceability table still has 13 stale rows: the 12 Phase-19 rows (ORCH-CON-01..04, MSG-WEBAPI-01..04, MSG-ACK-01..04) read 'Pending' despite [x] checkboxes + VERIFICATION SATISFIED; TEST-RMQ-05 reads 'Pending' + [ ] unchecked despite being Done (both Phase-20 AND Phase-21 close gates exit 0 with the triple-SHA snapshot). Doc-tracking lag, not a code gap. /gsd-complete-milestone typically reconciles these."
  - id: "PHASE-19-HUMAN-UAT-DISCHARGED"
    detail: "19-VERIFICATION status is human_needed (1 item: live cross-process correlation). That item was designed to be absorbed + automated by Phase 20 TEST-RMQ-02/CORR-04, now GREEN on the live stack. Discharged by automation; any 19-HUMAN-UAT doc still reading partial/pending is stale."
tech_debt:
  - phase: 17-messaging-contracts
    items:
      - "17-VALIDATION.md left in draft (nyquist_compliant: false) — same posture as the v3.3.0-deferred Phases 12/14/16 (passed VERIFICATION + close gate). /gsd-validate-phase 17 to close (optional)."
  - phase: 21-closeout-hygiene
    items:
      - "Phase 21 has no 21-VALIDATION.md (gap-closure phase; single forwarder refactor + 5 golden facts in L2ProjectionKeysTests). Covered by 21-VERIFICATION (6/6) + triple-SHA gate. Nyquist discovery flags it MISSING; /gsd-validate-phase 21 to close (optional)."
nyquist:
  compliant_phases: [18, 19, 20]
  partial_phases: [17]
  missing_phases: [21]
  overall: "3/5 compliant; Phase 17 VALIDATION draft (nyquist_compliant: false); Phase 21 (gap-closure) has no VALIDATION.md"
---

# Milestone v3.4.0 — Audit Report (BaseConsole + Orchestrator Messaging)

**Audited:** 2026-05-31 (rev 2, post-Phase-21) · **Status:** `tech_debt` (no blockers; the one code item from rev 1 is now closed — only doc/process hygiene remains) · **Scope:** Phases 17–21

> **Re-audit note:** This supersedes audit rev 1 (2026-05-31T01:00:00Z), which ran before Phase 21. Rev 1 carried one genuinely-open code item (WARNING-1, the duplicated L2 key shape) that became HARDEN-03 → Phase 21. **That item is now closed.** Phase 21 itself is folded into this audit's scope (40 requirements across Phases 17–21, up from rev 1's 37 across 17–20).

## Verdict

The milestone **achieved its definition of done**: a reusable `BaseConsole.Core` Generic-Host library and a runnable `Orchestrator` console, wired to the WebApi over MassTransit/RabbitMQ, with **per-stage CorrelationId observability proven end-to-end in Elasticsearch** (HTTP stage id → fresh publish-minted body Guid → orchestrator log scope), stopping cleanly at the scheduler-job-start seam. The triple-SHA close gate passes at **exit 0** (Phase 20: 3× consecutive 265-GREEN; Phase 21: 3× consecutive 270-GREEN; psql + redis + rabbitmq BEFORE==AFTER all held in both).

No requirement is `unsatisfied` → the **FAIL gate is not triggered**. Status is `tech_debt` rather than a clean `passed` solely because of one **verification-process gap** (Phase 20 has no VERIFICATION.md) plus traceability-doc staleness and two optional Nyquist-validation drafts — all non-blocking, all doc/process rather than code, all worth a glance before archive.

## Requirements Coverage (3-source cross-reference: VERIFICATION + SUMMARY frontmatter + traceability)

SUMMARY frontmatter in this project uses a `provides`/`requires` dependency-graph shape (not a `requirements_completed` list), so the per-phase **VERIFICATION.md SATISFIED tables are the authoritative source**, cross-checked against the traceability table and (for Phase 20) the close-gate + integration evidence.

| Phase | Reqs | VERIFICATION.md | Traceability | Final |
|-------|------|-----------------|--------------|-------|
| 17 | MSG-CONTRACTS-01..04, INFRA-RMQ-01 (5) | **passed** (9/9 truths, all 5 SATISFIED w/ code evidence) | 4× Pending, 1× Complete (stale) | **satisfied** (VERIFICATION authoritative; table lags) |
| 18 | CONSOLE-01..05, CONSOLE-HEALTH-01..04, CORR-01/02 (11) | **passed** (5/5 truths, 11/11 SATISFIED) | all Complete | **satisfied** |
| 19 | ORCH-CON-01..04, MSG-WEBAPI-01..04, MSG-ACK-01..04, INFRA-RMQ-02/03 (14) | **human_needed** (5/5 truths verified statically; 1 human item) | 12× Pending (stale), 2× Complete | **satisfied** — human item discharged by Phase 20 TEST-RMQ-02 (GREEN live) |
| 20 | CORR-03/04, TEST-RMQ-01..05 (7) | **MISSING** (only VALIDATION.md) | 6× Done, TEST-RMQ-05 Pending/[ ] (stale) | **satisfied (verification-doc gap)** — close gate exit 0 + integration PASS + Phase-21 270×3 GREEN incl. E2E are the evidence |
| 21 | HARDEN-01/02 (already done P18), HARDEN-03 (3) | **passed** (6/6 truths, HARDEN-03 SATISFIED; HARDEN-01/02 out-of-scope-satisfied P18) | all Complete | **satisfied** |

**40/40 requirements satisfied by evidence. 0 unsatisfied. 0 orphaned** — every REQ-ID maps to exactly one phase and appears in that phase's VERIFICATION SATISFIED table or — for Phase 20 — the close-gate + integration check + SUMMARY frontmatter.

## Cross-Phase Integration — PASS (gsd-integration-checker, re-run post-Phase-21)

**6/6 seams verified WIRED at source level · 0 BLOCKERs · 0 WARNINGs · 0 broken flows · 0 dependency-direction violations.**

1. **Publisher↔contract↔consumer** — both bind the same `Messaging.Contracts` types; correlation key is one shared constant (`CorrelationKeys.LogScope="CorrelationId"`). OK
2. **Per-stage correlation handoff** — publish mints fresh body Guid (`NewId.NextGuid()`); inbound filter reads body via `as ICorrelated`; HTTP `X-Correlation-Id` deliberately NOT carried to the bus body; E2E asserts `NotEqual(httpCorr, bodyGuid)` + same-Guid term-match across both ES docs. OK
3. **Dependency firewall** — no `BaseConsole.Core → BaseApi.Core`; WebApi never references BaseConsole.Core; both reference only Messaging.Contracts (verified via .csproj, both directions). OK
4. **RabbitMQ soft-dep asymmetry** — WebApi `MinimalFailureStatus=Degraded` keeps CRUD `/health/ready` 200 when broker down; Orchestrator `/health/ready` → Unhealthy. OK
5. **Redis L2 key single source of truth** — **[CHANGED & RE-VERIFIED in Phase 21]** writer `RedisProjectionKeys` AND reader `OrchestratorL2Keys` BOTH delegate to `Messaging.Contracts.Projections.L2ProjectionKeys`; the only surviving `$"{prefix}…"` interpolation literals in `src/` live exclusively in `L2ProjectionKeys.cs`; byte-shape `{prefix}{workflowId:D}` identical at both call sites. **WARNING-1 closed.** OK
6. **Compose tier** — rabbitmq 4.1.8-management-alpine (5673), redis, postgres, otel; both services `depends_on service_healthy`; ports/credentials/key-prefix consistent between compose env and appsettings. OK

## Nyquist Coverage

| Phase | VALIDATION.md | nyquist_compliant | Classification | Action |
|-------|---------------|-------------------|----------------|--------|
| 17 | exists (draft) | **false** | PARTIAL | `/gsd-validate-phase 17` (optional — matches v3.3.0-deferred posture) |
| 18 | exists (validated) | true | COMPLIANT | — |
| 19 | exists (draft) | true | COMPLIANT | — |
| 20 | exists (planned) | true | COMPLIANT | — |
| 21 | **missing** | — | MISSING | `/gsd-validate-phase 21` (optional — gap-closure forwarder refactor) |

## What's Strong

- The milestone's own DoD instrument — the triple-SHA close gate — **passes at exit 0** with full-stack 3×-consecutive GREEN (265 at Phase 20, 270 at Phase 21). Highest-confidence evidence available; covers the entire suite, not just one phase.
- The headline deliverable (per-stage correlation proven in Elasticsearch) is verified by a live cross-process E2E (`CorrelationPropagationE2ETests`), which also retires the only Phase 19 human-UAT item.
- The one genuinely-open code item from the first audit (WARNING-1 / L2 key duplication) is **closed and re-verified** — both forwarders now share a single source of truth, and the integration check confirms zero surviving literals.
- Integration check found **zero wiring breaks and zero warnings** across all five phases.

## What Needs a Glance Before Archive (none blocking)

1. **Phase 20 verification-doc gap** — no `20-VERIFICATION.md`. The evidence exists (gate + integration + SUMMARYs + Phase-21 E2E run); only the formal verifier pass is absent. Optional: `/gsd-verify-work 20`.
2. **Traceability staleness** — 13 rows in REQUIREMENTS.md lag reality (12 Phase-19 "Pending", TEST-RMQ-05 "Pending"/unchecked). Pure doc hygiene; `/gsd-complete-milestone` typically reconciles these.
3. **Phase 19 human-UAT** — verification reads `human_needed` though the item is discharged by TEST-RMQ-02 (GREEN live).
4. **Nyquist drafts** — Phase 17 VALIDATION draft (`nyquist_compliant: false`); Phase 21 has no VALIDATION.md. Both optional, both covered by VERIFICATION + close gate.

---
*Audited: 2026-05-31 (rev 2, post-Phase-21) · gsd-audit-milestone · integration check via gsd-integration-checker · supersedes rev 1*
