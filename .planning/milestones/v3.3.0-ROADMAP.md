# Milestone v3.3.0: Orchestration L3 → L1 → L2 Build Pipeline

**Status:** ✅ SHIPPED 2026-05-29
**Phases:** 12-16
**Total Plans:** 26

## Overview

On `POST /api/v1/orchestration/start`, the service now builds a transient in-memory (L1) representation of each requested workflow — fetched from Postgres (L3), validated for existence, cycles, schema-edge compatibility, and Payload↔ConfigSchema conformance — and projects it into a Redis (L2) materialized view that external consumers read. Stop is a Redis existence gate with GET-and-follow cleanup. Built on the shipped v3.2.0 foundation; no new SQL entities or columns (L3 schema is the v3.2.0 `InitialCreate` unchanged). Closes v2-deferred VALID-21 at orchestration-start scope.

## Phases

### Phase 12: Redis infra + composition + healthcheck + DI registration

**Goal**: Redis is a healthy compose-stack tier wired into the API's DI graph with test fixtures and dead-Redis resilience proven, while the Phase 5 HEALTH-01..05 contracts remain untouched.
**Depends on**: Nothing in v3.3.0 (builds on shipped v3.2.0 infrastructure)
**Requirements**: INFRA-REDIS-01..06, INFRA-COMP-01..04, TEST-REDIS-01..05
**Plans**: 8 plans

Plans:
- [x] 12-01: CPM pin StackExchange.Redis 2.13.1 + csproj references + OBSERV-REDIS-01 negative-grep
- [x] 12-02: compose.yaml redis service block (7.4.x-alpine, ping healthcheck) + depends_on
- [x] 12-03: appsettings Redis defaults section (KeyPrefix=skp:, JsonOptions=default)
- [x] 12-04: RedisProjectionOptions POCO + RedisServiceCollectionExtensions.AddBaseApiRedis + composition-root call #7
- [x] 12-05: RedisFixture + RedisFixtureFacts + Phase8WebAppFactory extension (TEST-REDIS-01..03)
- [x] 12-06: HealthDeadRedisFixture + 2 soft-dep acceptance facts (INFRA-REDIS-06 + TEST-REDIS-05)
- [x] 12-07: BaseApiCompositionFacts + RedisProjectionOptionsBindingFacts + ComposeYamlFacts + AppsettingsFacts
- [x] 12-08: Phase-close gate scripts (PowerShell + Bash) + 3-GREEN cadence + dual-SHA BEFORE=AFTER (TEST-REDIS-04)

**Details:** Redis added as a compose-stack tier (7.4.x-alpine, RSALv2/SSPLv1 — explicitly not AGPLv3 Redis 8.0+); `AddBaseApiRedis` registers `IConnectionMultiplexer` Singleton + `RedisProjectionOptions`; soft dependency (`/health/ready` stays 200 with Redis down); `RedisFixture` per-class `KeyPrefix` isolation with SCAN-assert-zero teardown (FLUSHDB forbidden); phase-close gate extended with `redis-cli --scan` SHA-256 BEFORE=AFTER.

### Phase 13: OrchestrationService split + L3 fetch + L1 build

**Goal**: `OrchestrationService` becomes a thin orchestrator; a transient `WorkflowGraphSnapshot` (L1) is loaded from Postgres on every Start request and disposed deterministically — no validation gates, no Redis write yet.
**Depends on**: Phase 12
**Requirements**: ORCH-SPLIT-01..04, L1-BUILD-01..05
**Plans**: 3 plans

Plans:
- [x] 13-01: Structural split: thin orchestrator (StartAsync+StopAsync) + 4 no-op seams + WorkflowGraphSnapshot record + DI
- [x] 13-02: Fill LoadL1Async: batched AsNoTracking reads + iterative cycle-terminating BFS + Mapperly ToRead + junction enrichment
- [x] 13-03: Tests: SC3 snapshot contents + SC5 fan-out/cycle termination + SC4 forced-throw deterministic disposal gate

**Details:** `StartAsync` delegates to 4 internal seams (`IWorkflowGraphLoader` + 3 validators + `IRedisProjectionWriter`) inside `Features/Orchestration/{Loading,Validation,Projection}/`; `StopAsync` extracted as a separate method; L1 snapshot is a flat `Dictionary<Guid, EntityDto>` per entity type loaded via batch `Where(x => ids.Contains(x.Id))` queries (Phase 3 5-method `IRepository<>` surface unchanged); `WorkflowGraphSnapshot.Dispose()` runs on both success and failure paths; multi-child fan-out traversal with per-traversal `visited` list keyed on StepId.

### Phase 14: Validation gates (DFS + schema-edge + payload-config-schema)

**Goal**: A broken workflow Start request returns a deterministic 422 + RFC 7807 at the first failed gate, with the offending entity ids in the error body and L1 cleanup guaranteed even on validation failure. Closes v2-deferred VALID-21 at orchestration-start scope.
**Depends on**: Phase 13
**Requirements**: L1-VALIDATE-01..10
**Plans**: 5 plans

Plans:
- [x] 14-01: 422 error path (OrchestrationValidationException + handler), D-04 split-Fallback, shared JsonSchemaConfig + SchemaDtoValidator refactor
- [x] 14-02: CycleDetector: two-set iterative DFS cycle + missing-step gate (+ CycleDetectionFacts, MissingStepFacts)
- [x] 14-03: SchemaEdgeValidator: independent edge walk, strict Guid equality, null-passes (+ SchemaEdgeFacts)
- [x] 14-04: PayloadConfigSchemaValidator: JsonSchema.Net evaluate + per-Start parse cache (+ PayloadConfigSchemaFacts)
- [x] 14-05: ValidationOrderFacts (gate-order short-circuit + L1 cleanup on failure) + regression sweep + STATE traceability

**Details:** Validation runs in locked order existence → cycles → schema-edge → Payload↔ConfigSchema; cycle detection uses ITERATIVE DFS with explicit stack (no recursion — `StackOverflowException` is uncatchable by `IExceptionHandler`); missing-step → 422 with (parentStepId, missingChildId); schema-edge mismatch (strict `Guid` equality, null-on-either-side passes) → 422; Payload validated against resolved `ConfigSchema` (JsonSchema.Net draft 2020-12, SSRF-locked, per-Start parse cache); L1 cleanup in `finally` on every failure path.

### Phase 15: L2 Redis projection write + Stop existence check

**Goal**: After a successful Start, the 3 Redis keyspaces are populated with the locked DTO shapes; Stop is a Redis-based existence gate that returns 422 (with the full missing list, no deletion) if any root is absent, else runs GET-and-follow cleanup (deletes root + per-step keys, never processor keys) and returns 204; repeated Stop → 422 (non-idempotent).
**Depends on**: Phase 14
**Requirements**: L2-PROJECT-01..07, ORCH-START-01..08, ORCH-STOP-01..07, OBSERV-REDIS-01..04
**Plans**: 5 plans

Plans:
- [x] 15-01: RedisProjectionKeys + 4 camelCase projection records + ProcessorKeyTtlDays config (Wave 0 unit tests)
- [x] 15-02: Fill RedisProjectionWriter: 3-keyspace batch write + processor TTL + liveness via TimeProvider
- [x] 15-03: Shared RedisL2Cleanup GET-and-follow traverse+delete routine + 422 missing-roots exception
- [x] 15-04: OrchestrationService per-workflow Start loop + Redis Stop gate/cleanup + DI/controller wiring
- [x] 15-05: OBSERV-REDIS-02 E2E + discipline guards + Phase 14 ValidationOrderFacts amend + REQUIREMENTS/ROADMAP reconcile

**Details:** 3 keyspaces — root `{ entryStepIds[], cron, jobId, liveness, correlationId }`, per-step `{ entryCondition, processorId, payload, nextStepIds[] }`, per-processor `{ inputDefinition, outputDefinition, liveness }` (exact field names, NOT `definitionIn`/`definitionOut`); per-processor keys TTL'd (`ProcessorKeyTtlDays` default 100), root/step keys no TTL; System.Text.Json (not Mapperly) for L2 serialization; `IServer.Keys()`/`KEYS` forbidden in production — Stop cleanup is targeted GET-and-follow traversal; Redis faults → 500 + RFC 7807 + offending op name via `exception.Data["redisOp"]`. **Stop scope evolved across two revisions** (2026-05-28 reduced to existence-check-only; Phase 15 CONTEXT D-04/D-06 re-added root+step deletion, never processor keys, collect-all-missing gate).

### Phase 16: Idempotency + concurrency + L1 cleanup + 3-GREEN closeout

**Goal**: Start idempotency, concurrent-Start last-write-wins semantics, Stop idempotency, and end-to-end happy-path are all verified against real Postgres + real Redis; the v3.3.0 phase-close gate (3 consecutive GREEN + `psql \l` SHA-256 BEFORE=AFTER + `redis-cli --scan` SHA-256 BEFORE=AFTER) is satisfied.
**Depends on**: Phase 15
**Requirements**: TEST-REDIS-06..09
**Plans**: 5 plans

Plans:
- [x] 16-01: Doc-first amendment (D-05): rewrite REQUIREMENTS.md TEST-REDIS-09 + ROADMAP SC2/SC5 to the inverted Stop contract; resolve the flagged NOTE
- [x] 16-02: TEST-REDIS-06 HappyPathE2EFacts: full-HTTP Start happy-path, 3-keyspace System.Text.Json round-trip
- [x] 16-03: TEST-REDIS-07 GateNoWriteFacts: 3 HTTP gates each 422 + RFC 7807 + SCAN-zero no-write; missing-step arm resolved
- [x] 16-04: TEST-REDIS-08 IdempotencyFacts (second-write-reflected + concurrent observational) + TEST-REDIS-09 thin StopScanFacts
- [x] 16-05: D-06 phase-close gate: copy phase-16-close.{ps1,sh}, run 3-GREEN + dual-SHA BEFORE=AFTER

**Details:** Full Start happy-path asserts all 3 keyspaces via System.Text.Json round-trip; each gate-failure path returns 422 AND SCAN confirms no L2 keys written; re-Start reflects second write (jobId changed) with orphan GC; concurrent Starts both 204 (last-write-wins, no Redis lock); Stop-after-Start removes root+step, keeps processor (TTL'd); Stop-without-Start → 422; repeated Stop → 422 (non-idempotent). Close gate: 3× consecutive GREEN at **235 passed**, byte-identical `psql \l` SHA-256 and `redis-cli --scan` SHA-256 BEFORE=AFTER, EF-migration + HEALTH-01..05 byte-immutable guards clean.

---

## Milestone Summary

**Key Decisions:**

- **Redis as a soft dependency** — `/health/ready` does NOT probe Redis; CRUD endpoints serve 200 with Redis down; only orchestration Start/Stop fail (500 + RFC 7807). Phase 5 HEALTH-01..05 contracts untouched.
- **Iterative DFS for cycle detection** (not recursive) — `StackOverflowException` is uncatchable by `IExceptionHandler`; explicit stack required.
- **Strict Schema.Id equality for schema-edge compatibility** — structural/canonical compatibility deferred; null-on-either-side passes (preserves Phase 10 source/sink/unconfigured semantics).
- **System.Text.Json (not Mapperly) for L2 serialization** — Mapperly is entity↔DTO only; L2 DTO → JSON string uses STJ directly.
- **`IServer.Keys()`/`KEYS` forbidden in production; targeted GET-and-follow traversal** — Stop cleanup walks the step graph by `StringGetAsync`, never enumerates the keyspace.
- **Stop scope evolved (two revisions)** — original mirror-of-Start eviction → existence-check-only (2026-05-28) → root+step deletion (never processor keys) + collect-all-missing gate (Phase 15 D-04/D-06). Non-idempotent by design.
- **Accepted evolved `psql \l` baseline** (`37b27e56…`) over the stale Phase-12 frozen literal at the Phase 16 close gate — the gate's true invariant is BEFORE==AFTER stability, which held; operator-approved.

**Issues Resolved:**

- Closed v2-deferred VALID-21 at orchestration-start scope (Payload↔ConfigSchema conformance gate).
- SSRF lockdown extended to the new Payload validator via shared `JsonSchemaConfig.DefaultOptions` (Phase 8 defense-in-depth preserved).
- Phase 14 verification debt (human_needed → verified) and Phase 15 human-UAT both reconciled before milestone close.

**Issues Deferred (intentional, documented):**

- **OBSERV-REDIS-04** — Redis-side profiling metrics → Prometheus (FUTURE-OTEL-REDIS).
- **FUTURE-STOP-EVICTION** — full Stop-side eviction with processor-key reference-counting.
- **FUTURE-SCHEDULER** — real `JobId`/`Liveness` write semantics (Scheduler contract).
- **FUTURE-GENERATION-ID** — monotonic version on the L2 root DTO for stale-projection detection.
- **FUTURE-VALID-21-HTTP-WRITE** — VALID-21 at Assignment-PUT/POST time (orchestration-start only in v3.3.0).

**Technical Debt Incurred:**

- Close-gate scripts (`scripts/phase-16-close.{ps1,sh}`): `-1` fact-count fallback, Docker Compose `ps --format json` array assumption, and PS1/SH service-list divergence — maintenance concerns only; do not affect pipeline correctness.
- Nyquist VALIDATION.md files for phases 12, 14, 16 left in `draft` (not marked compliant) — all three passed VERIFICATION and the close gate ran 3×235 GREEN against real backends.

---

_For current project status, see .planning/ROADMAP.md_
