---
milestone: v6.0.0
milestone_name: Config & Payload Validation Hardening
audited: 2026-06-13
status: passed
scope_phases: [56, 57, 58]
scores:
  requirements: 10/10
  phases: 3/3
  integration: live-proven (Phase-58 close gate N=3 GREEN)
  flows: 2/2 (CFG-08 block-422, CFG-09 start-204)
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 57-startup-config-schema-fetch-gate-a
    items:
      - "INFO (REVIEW IN-01): ConfigSchemaCoverageCheck.cs:379 — JSON-schema tuple `items`/`prefixItems` array form not walked. Conservative — tuple-item clashes missed but never false-positive; config schemas rarely use tuple validation."
      - "INFO (REVIEW IN-02): ConfigSchemaCoverageCheck.cs:98 — `$ref`/`allOf`/`oneOf`/`anyOf` composition keywords not followed (SSRF-safe by design). Conservative — composition-path clashes default to FINE."
      - "INFO (REVIEW IN-03): ConfigSchemaCoverageCheck.cs:130 — BuildClrLookup last-writer-wins on duplicate JSON name; shadowed-property iteration order undefined. Non-blocking for the zero-field ProcessorConfig base."
  - phase: 58-orchestration-gate-integration-proof-close
    items:
      - "INFO: BadConfigProcessor.cs:27 — trivial dead-path ProcessAsync transform. Intentional & documented — Gate A withholds the queue bind so the transform is never reached (dead-but-must-compile)."
doc_tracking_drift:  # bookkeeping only — no implementation gap
  - "57-03-SUMMARY.md requirements-completed omits CFG-05 (lists CFG-03/04/06/07). CFG-05 manually verified SATISFIED: ConfigSchemaCoverageCheck.cs (440 lines) wired at ProcessorStartupOrchestrator.cs:184; 18-row ConfigSchemaCoverageFacts theory green."
  - "ROADMAP.md shows 57-03-PLAN.md checkbox `[ ]` (unchecked) despite full execution (commits 7275c7f, d0f636f; 57-03-SUMMARY records completion)."
nyquist:  # discovery-only / advisory — never auto-validates
  compliant_phases: [56]
  partial_phases: [57, 58]
  missing_phases: []
  overall: partial-advisory
  note: "57/58 VALIDATION.md are validation-STRATEGY docs whose per-task status columns were never flipped post-execution (57 wave_0_complete:false with tasks ⬜ pending; 58 status:draft, nyquist_compliant:false, approval pending). Actual test evidence is GREEN per both VERIFICATION.md files (57: 561 hermetic passed; 58: 568×3 live N=3 GREEN). Backfill candidates only — does not block milestone close."
---

# v6.0.0 Milestone Audit — Config & Payload Validation Hardening

**Audited:** 2026-06-13
**Status:** ✅ **PASSED**
**Scope:** Phases 56, 57, 58 (10 CFG requirements; earlier phases belong to shipped milestones v3.x–v5.0.0)

## Definition of Done

Guarantee that any processor reaching *Healthy* can deserialize every orchestration-admitted payload, by enforcing the transitive invariant:

> `payload ⊨ ConfigSchemaId` (Gate B, exists) ∧ `ConfigSchemaId ⊨ configType` (Gate A, new) ⟹ payload deserializes.

**Verdict:** Achieved and **live-proven** end-to-end. The typed base-config seam (Phase 56) makes Gate A possible; Gate A (Phase 57) withholds `MarkHealthy` on a config-type↔config-schema clash; Phase 58 proves E2E against the real stack that a never-Healthy processor is blocked 422 at orchestration start while a compatible one starts 204.

## Requirements Coverage (3-Source Cross-Reference)

Sources: phase **VERIFICATION.md** status · **SUMMARY.md** `requirements-completed` frontmatter · **REQUIREMENTS.md** traceability checkbox.

| REQ | Description | Phase | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS | Final |
|-----|-------------|-------|--------------|---------------------|--------------|-------|
| CFG-01 | Typed base-config seam (framework deserializes payload→typed config) | 56 | passed | ✓ 56-01/02 | `[x]` | **satisfied** |
| CFG-02 | Processor.Sample migrated to typed seam (clean break) | 56 | passed | ✓ 56-01/02 | `[x]` | **satisfied** |
| CFG-03 | Startup fetches config-schema definition over bus | 57 | passed | ✓ 57-03 | `[x]` | **satisfied** |
| CFG-04 | Missing config-schema definition is transient (retry) | 57 | passed | ✓ 57-03 | `[x]` | **satisfied** |
| CFG-05 | Gate A: concrete config type covers config-schema definition | 57 | passed | ⚠ **omitted** | `[x]` | **satisfied** ¹ |
| CFG-06 | Gate A clash → never Healthy (MarkHealthy withheld, terminal) | 57 | passed | ✓ 57-03 | `[x]` | **satisfied** |
| CFG-07 | Null ConfigSchemaId skips Gate A (null-is-skip) | 57 | passed | ✓ 57-03 | `[x]` | **satisfied** |
| CFG-08 | Incompatible processor blocked 422 at orchestration start (E2E) | 58 | passed | ✓ 58-01/04/05 | `[x]` LIVE-PROVEN | **satisfied** |
| CFG-09 | Compatible processor → Healthy → starts 204 (no false-positive) | 58 | passed | ✓ 58-02/04/05 | `[x]` LIVE-PROVEN | **satisfied** |
| CFG-10 | TOCTOU closed — frozen-once-referenced schema immutability | 57 | passed | ✓ 57-04 | `[x]` | **satisfied** |

**Score: 10/10 satisfied. No unsatisfied, no orphaned requirements.**

¹ **CFG-05** is missing from every SUMMARY `requirements-completed` list (57-02 deliberately deferred marking to "avoid over-claiming a partially-wired requirement"; 57-03 then listed CFG-03/04/06/07 and forgot CFG-05). Per the status matrix this drops to *partial — verify manually*. **Manual verification performed and confirms SATISFIED:** `ConfigSchemaCoverageCheck.cs` (440 lines) exists and is wired at `ProcessorStartupOrchestrator.cs:184`; `ConfigSchemaCoverageFacts` 18-row theory is green (VERIFICATION 57 SC2). The omission is bookkeeping drift, not an implementation gap.

## Phase Verifications

| Phase | Status | Score | Evidence highlight |
|-------|--------|-------|--------------------|
| 56 — Typed Base-Config Seam | passed | 6/6 | 530/530 hermetic green; old raw-string seam absent (grep=0); Release+Debug 0-warning |
| 57 — Startup Config-Schema Fetch + Gate A | passed | 5/5 | 561 hermetic green; 18-row covers theory + 3 freeze facts; CFG-08/09 correctly deferred to 58 |
| 58 — Orchestration-Gate Integration Proof & Close | passed | 3/3 | Live N=3 GREEN close gate (exit 0); 568 facts ×3; triple-SHA BEFORE==AFTER net-zero |

All three VERIFICATION.md files present and `status: passed`. No unverified phases.

## Cross-Phase Integration

> The `gsd-integration-checker` subagent is not installed in this environment; integration was checked inline by the orchestrator.

The v6.0.0 integration chain — **Phase 56 typed seam → Phase 57 Gate A covers-check → Phase 58 orchestration-start liveness composition** — is exactly what Phase 58's close gate proves end-to-end against the real stack:

- **CFG-08 (block path):** `Processor.BadConfig` (Phase-56 typed seam) → Gate A clash (Phase-57 `ConfigSchemaCoverageCheck`) → `MarkHealthy` withheld → no `skp:{badId}` L2 key → existing `ProcessorLivenessValidator` throws `ProcessorNotLive("absent")` → orchestration start **422**. Three-signal causation: ES clash log scoped to `service.name=processor-badconfig` + `skp:{badId}` stably absent (3×5s > 10s heartbeat) + 422.
- **CFG-09 (negative control):** compatible `Processor.Sample` → Gate A pass → Healthy → `skp:{sampleId}` written → orchestration start **204**.

Both proven live (`GateACompositionE2ETests`, N=3 GREEN). Gate B (`PayloadConfigSchemaValidator`) retained unchanged per scope. **No cross-phase wiring gaps. No broken flows.**

## End-to-End Flows

| Flow | Result |
|------|--------|
| Config-incompatible processor blocked at orchestration start (422) | ✅ live-proven |
| Config-compatible processor reaches Healthy and starts normally (204) | ✅ live-proven |

## Tech Debt (non-critical, advisory)

**Conservative coverage-checker gaps** (`ConfigSchemaCoverageCheck.cs`, all INFO, documented in REVIEW.md, conservative-by-design → never false-positive):
- IN-01: tuple `items`/`prefixItems` array form not walked.
- IN-02: `$ref`/`allOf`/`oneOf`/`anyOf` composition keywords not followed (SSRF-safe).
- IN-03: `BuildClrLookup` last-writer-wins on duplicate JSON name (non-blocking for zero-field base).

**Intentional dead path:** `BadConfigProcessor.ProcessAsync` is a never-reached transform (Gate A blocks the bind) — documented, not a stub.

**Documentation-tracking drift** (bookkeeping only — no behavior impact):
- 57-03-SUMMARY `requirements-completed` omits CFG-05 (verified satisfied above).
- ROADMAP.md 57-03-PLAN checkbox left `[ ]` despite execution.
- 57/58 VALIDATION strategy docs' per-task status columns never flipped post-execution.

## Nyquist Compliance (discovery-only — advisory)

| Phase | VALIDATION.md | nyquist_compliant | Classification |
|-------|---------------|-------------------|----------------|
| 56 | exists (`validated`) | true | **COMPLIANT** |
| 57 | exists (`ready`) | true (tasks ⬜ pending, wave_0_complete:false) | **PARTIAL** |
| 58 | exists (`draft`) | false (approval pending) | **PARTIAL** |

These are validation-*strategy* artifacts whose status columns were not updated after execution. The underlying test evidence is GREEN in both VERIFICATION.md files. Backfill candidates (optional): `/gsd-validate-phase 57`, `/gsd-validate-phase 58`. **Advisory only — does not block close.**

## Conclusion

All 10/10 CFG requirements satisfied with live proof. Three phases verified passed. Cross-phase integration and both E2E flows live-proven by the Phase-58 N=3 GREEN close gate with triple-SHA net-zero. Remaining items are INFO-level conservative-by-design code notes, one intentional dead path, and documentation-tracking drift — **minimal tech debt, no critical blockers**.

**Milestone v6.0.0 is ready to complete.**

---

_Audited: 2026-06-13 · gsd-audit-milestone_
