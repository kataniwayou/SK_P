---
milestone: v3.6.0
milestone_name: Idempotent Execution — Exactly-Once-Effect Round-Trip
audited: 2026-06-05
status: tech_debt
scores:
  requirements: 14/14 active (Phase-31 req-1..8 + Phase-32.1 req-1..6; Phase-32 req-1..8 retired/superseded)
  phases: 3/3 active verified (31 passed, 31.1 gate-complete, 32.1 passed+gate; 32 superseded)
  integration: 5/5
  flows: 2/2
gaps: {}   # no functional blockers — no unsatisfied requirements, no integration gaps, no broken flows
tech_debt:
  - phase: planning-hygiene (milestone-wide)
    items:
      - "REQUIREMENTS.md is stale at v3.5.0 — the v3.6.0 requirements (Phase-31 req-1..8, Phase-32.1 req-1..6) were never rolled into the global traceability table; they live only in the per-phase SPEC.md files. /gsd-complete-milestone should roll REQUIREMENTS.md forward."
      - "ROADMAP.md ## Milestones bullet still shows v3.6.0 as '🚧 Phase 31+ (planning)' — stale; should be ✅ shipped. /gsd-complete-milestone fixes this."
  - phase: 31-idempotent-execution-exactly-once-effect
    items:
      - "31-VALIDATION.md frontmatter is status: draft, nyquist_compliant: false — a stale plan-time artifact. The phase is VERIFICATION passed 8/8 and its live close gate PASSED (via 31.1). Validation never finalized to validated-partial."
      - "No 31-SECURITY.md — security threat verification was never run for Phase 31 (idempotency hashing / L2 key surface)."
  - phase: 31.1-close-gate-net-zero
    items:
      - "No 31.1-VERIFICATION.md — gap-closure phase; its verification IS the live close gate (PASSED, recorded in 31.1-SUMMARY status: complete). Verification-artifact debt only."
      - "No 31.1-VALIDATION.md and no 31.1-SECURITY.md (gap-closure phase; script-only)."
  - phase: 32-cancelled-circuit-breaker
    items:
      - "SUPERSEDED by 32.1 (breaker reverted). Its 32-VERIFICATION.md still reads status: human_needed (7/8, pending the breaker live gate) and 32-VALIDATION.md reads validated-partial pending the operator breaker gate — both reference a breaker that no longer exists in the codebase. Cleanup: mark Phase 32 retired/superseded in its artifacts so it stops reading as 'pending'."
      - "32-HUMAN-UAT.md already resolved by supersession (done during 32.1 execution); the VERIFICATION/VALIDATION artifacts were not similarly updated."
  - phase: 32.1-dead-letter-on-exhaustion
    items:
      - "32.1-VALIDATION.md nyquist_compliant: false (validated-partial) — by design: 3 real-stack behaviors are legitimately manual-only (operator gate). hermetic_complete: true, 0 fillable gaps. Now also live-proven (close gate + smoke tests), so the manual-only items are satisfied; the flag stays false only because nyquist_compliant means 'all automated'."
nyquist:
  compliant_phases: []
  partial_phases: ["31 (draft)", "32 (validated-partial, superseded)", "32.1 (validated-partial, hermetic_complete)"]
  missing_phases: ["31.1 (gap-closure, script-only)"]
  overall: partial
security:
  verified_phases: ["32 (threats_open 0)", "32.1 (threats_open 0, status verified)"]
  missing_phases: ["31", "31.1"]
---

# v3.6.0 Milestone Audit — Idempotent Execution (Exactly-Once-Effect Round-Trip)

> **Status: tech_debt** — All active requirements are satisfied and LIVE-verified; cross-phase
> integration is 5/5 with zero gaps; both E2E flows pass live. There are **no functional blockers**.
> What remains is accumulated **planning-hygiene / artifact debt** (stale REQUIREMENTS.md at the wrong
> milestone, stale ROADMAP milestone bullet, a superseded Phase-32 whose verification artifacts still
> read "pending", missing SECURITY.md on phases 31/31.1, and a stale draft 31-VALIDATION.md). Most of
> it is exactly what `/gsd-complete-milestone` cleans up.

## Scope

| Phase | Title | Plans | Status | Verification |
|-------|-------|-------|--------|--------------|
| 31 | Idempotent Execution Round-Trip (Exactly-Once-Effect) | 6/6 | Complete | **passed 8/8** (req-8 net-zero → 31.1) |
| 31.1 | Close-Gate Redis Net-Zero (gap closure) | 1/1 | Complete | live close gate **PASSED** (no VERIFICATION.md — gate is the proof) |
| 32 | Cancelled Circuit-Breaker | 5/6 | **Superseded by 32.1** | human_needed (moot — breaker reverted) |
| 32.1 | Dead-Letter on Exhaustion (Breaker Reverted) | 2/2 | Complete | **passed 6/6** + live close gate **GATE_EXIT=0** |

`REQUIREMENTS.md` is stale at v3.5.0, so requirements are taken from the per-phase SPEC.md files
(`req-1..req-N`) cross-referenced against each phase's VERIFICATION.md and SUMMARY frontmatter.

## Requirements Coverage (3-source cross-reference)

All **14 active requirements satisfied**. Phase-32's 8 requirements are **retired** (the breaker was a
conscious milestone decision that was reverted — 32-REVIEW.md WR-01; the user took "stop the job" off
the table). The integration checker independently confirmed all 14 are wired in the actual source.

| Req set | VERIFICATION | SUMMARY frontmatter | Integration | Final |
|---------|--------------|---------------------|-------------|-------|
| Phase-31 req-1..8 | passed 8/8 | listed | 8/8 WIRED | **satisfied** |
| Phase-31.1 NET-ZERO-31 / req-8b | live gate PASSED | listed `[NET-ZERO-31, req-8b]` | (redis net-zero held) | **satisfied** |
| Phase-32.1 req-1..6 | passed 6/6 | listed | 6/6 WIRED | **satisfied** |
| Phase-32 req-1..8 | human_needed | — | n/a (code removed) | **retired (superseded)** |

**Orphan / unsatisfied requirements: none.** (Caveat: orphan detection against REQUIREMENTS.md is
inapplicable here because the table is stale at v3.5.0 — see tech debt.)

## Cross-Phase Integration — 5/5, zero gaps

The decisive question for this milestone — *did Phase 32.1's net-deletion revert preserve Phase 31's
exactly-once-effect idempotency?* — is **YES**, verified against the actual codebase:

1. Processor `flag[dispatch.H]=="Ack"` dedup gate + `DispatchDeduped` counter — **WIRED**, byte-intact.
2. Orchestrator `flag[m.H]=="Ack"` dedup gate + `ResultDeduped` counter + manifest N×M fan-out — **WIRED**.
3. Dead-letter mechanism `UseMessageRetry(Immediate(retryLimit))` + `Configure<RetryOptions>` (×2) — **WIRED** (the breaker's replacement).
4. Zero dangling breaker symbols across `src/` + `tests/` (Cancelled / CancelledMarkerValue / WorkflowCancelled / FaultUnscheduleConsumer / GetRetryAttempt) — **CLEAN**.
5. Content-addressed round-trip producer↔consumer (HashBlob → data[blobHash] → manifest → ExecutionResult.H → ResultConsumer flag[m.H] dedup → fan-out) — **WIRED**, full chain.

## E2E Flows — 2/2 live-GREEN

- **Idempotent exactly-once-effect round-trip** — `IdempotentExactlyOnceE2ETests` (induced redelivery, zero downstream dup) ran live inside the Phase-32.1 close gate's 3×GREEN = **452 facts**, alongside Phase-31's revert. Phase-31's idempotency E2E and Phase-32.1's revert pass in the SAME live run.
- **Dead-letter on exhaustion** — live smoke test: `processor_dispatch_consumed` +4 (`Immediate(3)`+1) on Prometheus, `{procId}_error` queue 0→1 on RabbitMQ, ES infra-fault log, ZERO `skp:cancelled:*` markers.

Both close gates (31.1 and 32.1) held the triple-SHA (psql `\l` / redis `--scan` / rabbitmqctl
`list_queues`) BEFORE==AFTER — redis net-zero, no key leak.

## Nyquist — partial (acceptable)

| Phase | VALIDATION.md | nyquist_compliant | Classification |
|-------|---------------|-------------------|----------------|
| 31 | exists (status: draft) | false | PARTIAL (stale draft) |
| 31.1 | missing | — | MISSING (gap-closure, script-only) |
| 32 | exists (validated-partial) | false | PARTIAL (superseded) |
| 32.1 | exists (validated-partial) | false | PARTIAL (hermetic_complete; real-stack manual-only, now live-proven) |

Hermetic coverage is complete across the active phases; the non-compliance is the established
"real-stack behaviors are manual-only" pattern — and those manual-only items are now live-proven by
the close gates + smoke tests.

## Security — partial

32 and 32.1 have SECURITY.md (`threats_open: 0`). **31 and 31.1 have none** — Phase-31's idempotency
hashing / L2 key write surface was never threat-audited. Recommend `/gsd-secure-phase 31`.

## Verdict

The milestone **achieved its definition of done**: deterministic exactly-once-effect idempotency on
both hops (Phase 31) and clean dead-lettering on retry-budget exhaustion (Phase 32.1, superseding the
abandoned breaker), all proven live. The remaining work is artifact/doc hygiene, not functionality —
safe to complete the milestone, with the cleanup items tracked above (most are auto-handled by
`/gsd-complete-milestone`, which rolls REQUIREMENTS.md forward and updates the ROADMAP milestone bullet).
