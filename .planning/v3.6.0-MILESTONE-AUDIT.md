---
milestone: v3.6.0
milestone_name: Idempotent Execution — Exactly-Once-Effect Round-Trip
audited: 2026-06-05 (re-audit)
status: tech_debt
scores:
  requirements: 14/14 active (Phase-31 req-1..8 + Phase-32.1 req-1..6; Phase-32 req-1..8 retired/superseded)
  phases: 3/3 active verified (31 passed, 31.1 gate-complete, 32.1 passed+gate; 32 superseded)
  integration: 5/5
  flows: 2/2
gaps: {}   # no functional blockers — no unsatisfied requirements, no integration gaps, no broken flows
resolved_since_prior_audit:
  - "31-SECURITY.md created via /gsd-secure-phase 31 — 19/19 threats closed, threats_open 0 (T-31-08 EntryId-in-log-template remediated in-audit). Was a tech-debt item; now CLEARED."
  - "31-VALIDATION.md finalized via /gsd-validate-phase 31 — status: verified, nyquist_compliant: true, 8/8 requirements COVERED (hermetic re-run 445/0). Was a stale draft; now CLEARED."
tech_debt:
  - phase: planning-hygiene (milestone-wide)
    items:
      - "REQUIREMENTS.md is stale at v3.5.0 — the v3.6.0 requirements (Phase-31 req-1..8, Phase-32.1 req-1..6) were never rolled into the global traceability table; they live only in the per-phase SPEC.md files. /gsd-complete-milestone should roll REQUIREMENTS.md forward."
      - "ROADMAP.md ## Milestones bullet still shows v3.6.0 as '🚧 Phase 31+ (planning)' — stale; should be ✅ shipped. /gsd-complete-milestone fixes this."
  - phase: 31.1-close-gate-net-zero
    items:
      - "No 31.1-VERIFICATION.md — gap-closure phase; its verification IS the live close gate (PASSED, recorded in 31.1-SUMMARY status: complete). Verification-artifact debt only."
      - "No 31.1-VALIDATION.md and no 31.1-SECURITY.md (gap-closure phase; script-only — acceptable)."
  - phase: 32-cancelled-circuit-breaker
    items:
      - "SUPERSEDED by 32.1 (breaker reverted). Its 32-VERIFICATION.md still reads status: human_needed (7/8, pending the breaker live gate) and 32-VALIDATION.md reads validated-partial pending the operator breaker gate — both reference a breaker that no longer exists in the codebase. Cleanup: mark Phase 32 retired/superseded in its artifacts so it stops reading as 'pending'."
      - "32-HUMAN-UAT.md already resolved by supersession (done during 32.1 execution); the VERIFICATION/VALIDATION artifacts were not similarly updated."
  - phase: 32.1-dead-letter-on-exhaustion
    items:
      - "32.1-VALIDATION.md nyquist_compliant: false (validated-partial) — by design: 3 real-stack behaviors are legitimately manual-only (operator gate). hermetic_complete: true, 0 fillable gaps. Now also live-proven (close gate + smoke tests), so the manual-only items are satisfied; the flag stays false only because nyquist_compliant means 'all automated'."
nyquist:
  compliant_phases: ["31 (verified, nyquist_compliant true)"]
  partial_phases: ["32 (validated-partial, superseded)", "32.1 (validated-partial, hermetic_complete, live-proven)"]
  missing_phases: ["31.1 (gap-closure, script-only)"]
  overall: partial
security:
  verified_phases: ["31 (threats_open 0, status verified)", "32 (threats_open 0)", "32.1 (threats_open 0, status verified)"]
  missing_phases: ["31.1 (gap-closure, script-only — acceptable)"]
---

# v3.6.0 Milestone Audit — Idempotent Execution (Exactly-Once-Effect Round-Trip)

> **Status: tech_debt** — All active requirements are satisfied and LIVE-verified; cross-phase
> integration is **5/5 with zero gaps** (re-confirmed at current HEAD); both E2E flows pass live.
> There are **no functional blockers**. What remains is accumulated **planning-hygiene / artifact debt**
> (stale REQUIREMENTS.md at the wrong milestone, stale ROADMAP milestone bullet, a superseded Phase-32
> whose verification artifacts still read "pending", and missing gap-closure artifacts on 31.1). Most of
> it is exactly what `/gsd-complete-milestone` cleans up.

## What changed since the prior audit (2026-06-05)

Two Phase-31 tech-debt items flagged by the prior audit were **resolved this session**:

- **31-SECURITY.md created** (`/gsd-secure-phase 31`) — State-B audit, **19/19 threats closed**, `threats_open: 0`. One finding (T-31-08: EntryId interpolated into the input-absent log template) was **remediated in-audit** by dropping `{EntryId}` from the template; EntryId still emits as a scope value via `InboundExecutionScopeConsumeFilter`. `BaseProcessor.Core` rebuilt clean.
- **31-VALIDATION.md finalized** (`/gsd-validate-phase 31`) — was a stale plan-time draft (`status: draft`, `nyquist_compliant: false`); now `status: verified`, `nyquist_compliant: true`, **8/8 requirements COVERED**, hermetic/integration tier re-run **445 passed / 0 failed**.

The only source delta is the one log-template line in `EntryStepDispatchConsumer.cs` — a pure observability cleanup. The integration checker independently confirmed it touches **zero wiring, zero test assertions, zero metric/broker paths**.

## Scope

| Phase | Title | Plans | Status | Verification |
|-------|-------|-------|--------|--------------|
| 31 | Idempotent Execution Round-Trip (Exactly-Once-Effect) | 6/6 | Complete | **passed 8/8** (req-8 net-zero → 31.1); SECURITY ✓, VALIDATION ✓ |
| 31.1 | Close-Gate Redis Net-Zero (gap closure) | 1/1 | Complete | live close gate **PASSED** (no VERIFICATION.md — gate is the proof) |
| 32 | Cancelled Circuit-Breaker | 5/6 | **Superseded by 32.1** | human_needed (moot — breaker reverted) |
| 32.1 | Dead-Letter on Exhaustion (Breaker Reverted) | 2/2 | Complete | **passed 6/6** + live close gate **GATE_EXIT=0** |

`REQUIREMENTS.md` is stale at v3.5.0, so requirements are taken from the per-phase SPEC.md files
(`req-1..req-N`) cross-referenced against each phase's VERIFICATION.md and SUMMARY frontmatter.

## Requirements Coverage (3-source cross-reference)

All **14 active requirements satisfied** and independently confirmed WIRED in source by the integration
checker. Phase-32's 8 requirements are **retired** (the circuit-breaker was a conscious milestone
decision that was reverted — 32-REVIEW.md WR-01; the user took "stop the job" off the table).

| Req set | VERIFICATION | SUMMARY frontmatter | Integration | Final |
|---------|--------------|---------------------|-------------|-------|
| Phase-31 req-1..8 | passed 8/8 | listed (req-1..7; req-8 = live E2E) | 8/8 WIRED | **satisfied** |
| Phase-31.1 NET-ZERO-31 / req-8b | live gate PASSED | listed `[NET-ZERO-31, req-8b]` | redis net-zero held | **satisfied** |
| Phase-32.1 req-1..6 | passed 6/6 | listed | 6/6 WIRED | **satisfied** |
| Phase-32 req-1..8 | human_needed | — | n/a (code removed) | **retired (superseded)** |

**Orphan / unsatisfied requirements: none.** (Caveat: orphan detection against REQUIREMENTS.md is
inapplicable here because the table is stale at v3.5.0 — see tech debt.)

## Cross-Phase Integration — 5/5, zero gaps (re-confirmed at current HEAD)

The decisive question for this milestone — *did Phase 32.1's net-deletion revert preserve Phase 31's
exactly-once-effect idempotency?* — is **YES**, re-verified against the actual codebase:

1. Processor `flag[dispatch.H]=="Ack"` dedup gate + `DispatchDeduped` counter (`EntryStepDispatchConsumer.cs:76-84`) — **WIRED**, byte-intact; Pending→Ack flip (When.Exists/keepTtl) at `:225`.
2. Orchestrator `flag[m.H]=="Ack"` dedup gate + `ResultDeduped` counter + manifest N×M fan-out (`ResultConsumer.cs:65-115`) — **WIRED**.
3. Dead-letter replacement `UseMessageRetry(Immediate(retryLimit))` from `RetryOptions` at all 4 sites (`ProcessorStartupOrchestrator.cs:154`, `ResultConsumerDefinition.cs:41`, `StartOrchestrationConsumerDefinition.cs:38`, `StopOrchestrationConsumerDefinition.cs:33`) — **WIRED**.
4. Zero dangling breaker symbols across `src/` + `tests/` (Cancelled* / WorkflowCancelled / FaultUnscheduleConsumer / GetRetryAttempt / CircuitBreaker) — **CLEAN** (grep-confirmed zero; `StepOutcome.Cancelled` / `StepEntryCondition.PreviousCancelled` are legitimate EXEC-08 token-cancellation enums, not the breaker).
5. Content-addressed round-trip chain (HashBlob → data[blobHash] → manifest → ExecutionResult.H → ResultConsumer flag[m.H] dedup → fan-out) — **WIRED**, full chain through the single canonical `MessageIdentity` hash class.

## E2E Flows — 2/2 live-GREEN

- **Idempotent exactly-once-effect round-trip** — `IdempotentExactlyOnceE2ETests` (induced redelivery, zero downstream dup) ran live inside the Phase-32.1 close gate's 3×GREEN = **452 facts**, alongside Phase-31's revert. Phase-31's idempotency E2E and Phase-32.1's revert pass in the SAME live run.
- **Dead-letter on exhaustion** — live smoke test: `processor_dispatch_consumed` +4 (`Immediate(3)`+1) on Prometheus, `{procId}_error` queue 0→1 on RabbitMQ, ES infra-fault log, ZERO `skp:cancelled:*` markers.

Both close gates (31.1 and 32.1) held the triple-SHA (psql `\l` / redis `--scan` / rabbitmqctl
`list_queues`) BEFORE==AFTER — redis net-zero, no key leak.

## Nyquist — partial (acceptable; improved)

| Phase | VALIDATION.md | nyquist_compliant | Classification |
|-------|---------------|-------------------|----------------|
| 31 | exists (status: verified) | **true** | **COMPLIANT** (finalized this session — 445/0 hermetic) |
| 31.1 | missing | — | MISSING (gap-closure, script-only) |
| 32 | exists (validated-partial) | false | PARTIAL (superseded) |
| 32.1 | exists (validated-partial) | false | PARTIAL (hermetic_complete; real-stack manual-only, live-proven) |

Hermetic coverage is complete across the active phases; the residual non-compliance is the established
"real-stack behaviors are manual-only" pattern (32.1) plus the script-only gap-closure phase (31.1) —
and those manual-only items are live-proven by the close gates + smoke tests.

## Security — verified (improved)

| Phase | SECURITY.md | threats_open | Classification |
|-------|-------------|--------------|----------------|
| 31 | exists (status: verified) | **0** | **VERIFIED** (created this session — 19/19 closed) |
| 31.1 | missing | — | MISSING (gap-closure, script-only — acceptable) |
| 32 | exists | 0 | VERIFIED |
| 32.1 | exists (status: verified) | 0 | VERIFIED |

All implementing phases are now threat-verified with `threats_open: 0`. Only the script-only gap-closure
phase 31.1 lacks a SECURITY.md, which is acceptable (no new attack surface — it is a teardown/scan-clean
script change).

## Verdict

The milestone **achieved its definition of done**: deterministic exactly-once-effect idempotency on
both hops (Phase 31) and clean dead-lettering on retry-budget exhaustion (Phase 32.1, superseding the
abandoned breaker), all proven live. Since the prior audit, Phase-31's security and validation artifact
debt has been **cleared** (31-SECURITY.md threats_open 0; 31-VALIDATION.md verified/compliant), and
cross-phase integration was **re-confirmed 5/5 at current HEAD**. The remaining work is
artifact/doc hygiene, not functionality — safe to complete the milestone, with the cleanup items
tracked above (most are auto-handled by `/gsd-complete-milestone`, which rolls REQUIREMENTS.md forward
and updates the ROADMAP milestone bullet).
