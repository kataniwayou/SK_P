---
phase: 71-orchestrator-recovery-pipeline
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/Messaging.Contracts/OrchestratorInject.cs
  - src/Messaging.Contracts/OrchestratorReinject.cs
  - tests/BaseApi.Tests/Contracts/OrchestratorContractTests.cs
autonomous: true
requirements: [ORCV-06]
must_haves:
  truths:
    - "OrchestratorInject and OrchestratorReinject exist as sealed records implementing IKeeperRecoverable"
    - "OrchestratorReinject carries a StepOutcome discriminator plus the result-field superset (ErrorMessage, CancellationMessage)"
    - "Both contracts partition correctly through the existing 4-tuple ReinjectConsumerDefinition.PartitionGuid (no helper change)"
  artifacts:
    - path: "src/Messaging.Contracts/OrchestratorInject.cs"
      provides: "Keeper-recovery contract for the orchestrator FORWARD escalation (completes copy + dispatch)"
      contains: "record OrchestratorInject"
    - path: "src/Messaging.Contracts/OrchestratorReinject.cs"
      provides: "Keeper-recovery contract carrying the outcome discriminator for IStepResult reconstruction"
      contains: "record OrchestratorReinject"
    - path: "tests/BaseApi.Tests/Contracts/OrchestratorContractTests.cs"
      provides: "Round-trip + IKeeperRecoverable + partition-key facts for the two new contracts"
      contains: "OrchestratorReinject"
  key_links:
    - from: "src/Messaging.Contracts/OrchestratorReinject.cs"
      to: "Messaging.Contracts.StepOutcome"
      via: "Outcome { get; init; } discriminator field"
      pattern: "StepOutcome Outcome"
    - from: "src/Messaging.Contracts/OrchestratorInject.cs"
      to: "Messaging.Contracts.IKeeperRecoverable"
      via: "interface implementation (5-id base)"
      pattern: "IKeeperRecoverable"
---

<objective>
Add the two new origin-split keeper-recovery contracts: `OrchestratorInject` and `OrchestratorReinject`,
both implementing `IKeeperRecoverable` so the existing 4-tuple partitioner works unchanged. Mirror the
`ProcessorInject`/`ProcessorReinject` (née `Keeper*`) record shape exactly; the only divergence is
`OrchestratorReinject`'s D-07 outcome discriminator + result-field superset.

Purpose: These are the wire contracts the pipeline (Plan 03) builds and the two new consumers (Plan 04)
handle. Defining them first gives both downstream plans a stable contract to compile against (interface-first
ordering).

Output: Two contract records + a contract-test fact file. The solution builds 0-warning.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-orchestrator-recovery-pipeline/71-CONTEXT.md
@.planning/phases/71-orchestrator-recovery-pipeline/71-PATTERNS.md
@.planning/phases/71-orchestrator-recovery-pipeline/71-RESEARCH.md

<interfaces>
<!-- The contract analogs are the Plan-01-renamed Processor* files. Copy their shape verbatim. -->
<!-- IKeeperRecoverable (the 5-id partition marker), StepOutcome (the discriminator enum), and the four -->
<!-- IStepResult subtypes' union fields are the source-of-truth types. -->

src/Messaging.Contracts/IKeeperRecoverable.cs — the 5-id partition marker the partitioner keys on:
  Guid CorrelationId, WorkflowId, ProcessorId, ExecutionId, EntryId (the 4-tuple corr:wf:proc:exec drives PartitionGuid).

src/Messaging.Contracts/StepOutcome.cs (values 0-3): Completed, Failed, Cancelled, Processing.

IStepResult union fields (verified): StepCompleted/StepProcessing carry NO diagnostic field;
  StepFailed.ErrorMessage (string), StepCancelled.CancellationMessage (string) are the only union fields.

Convention (every record in src/Messaging.Contracts/): NO [JsonPropertyName] — default STJ. (Only a
  positional-record property would need [property: JsonPropertyName]; these use init-only props, not positional.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OrchestratorInject and OrchestratorReinject contract records</name>
  <read_first>
    - src/Messaging.Contracts/ProcessorInject.cs (Plan-01 rename of KeeperInject.cs — the exact analog shape to copy)
    - src/Messaging.Contracts/ProcessorReinject.cs (Plan-01 rename of KeeperReinject.cs — analog)
    - src/Messaging.Contracts/IKeeperRecoverable.cs (the 5-id marker both contracts implement)
    - src/Messaging.Contracts/StepOutcome.cs (the discriminator enum, values 0-3)
    - src/Messaging.Contracts/StepFailed.cs (ErrorMessage union field)
    - src/Messaging.Contracts/StepCancelled.cs (CancellationMessage union field)
  </read_first>
  <action>
    Create src/Messaging.Contracts/OrchestratorInject.cs — copy the ProcessorInject record verbatim and
    adapt the field set to what the orchestrator INJECT consumer needs to COMPLETE the index+data copy and
    dispatch downstream (the FORWARD-Post tail the pipeline couldn't finish). Per PATTERNS.md it carries the
    slot-tuple fields plus the copy operands. Exact field set is Claude's discretion within D-07, but it MUST
    include: the 5-id base (CorrelationId, WorkflowId, ProcessorId, ExecutionId, EntryId) via IKeeperRecoverable,
    the downstream-dispatch fields (NextStepId, NextProcessorId, Payload), the new data key to copy INTO
    (newEntryId — may reuse EntryId for this, document the choice), and the origin entry id to copy FROM
    (OriginEntryId). Example shape:

        public sealed record OrchestratorInject(Guid WorkflowId, Guid StepId, Guid ProcessorId) : IKeeperRecoverable
        {
            public Guid CorrelationId { get; init; }
            public Guid ExecutionId   { get; init; }
            public Guid EntryId       { get; init; }   // the newEntryId to copy-into / dispatch with
            public Guid OriginEntryId { get; init; }   // the origin data key to copy FROM
            public Guid NextStepId    { get; init; }
            public Guid NextProcessorId { get; init; }
            public string Payload     { get; init; } = "";
        }

    Create src/Messaging.Contracts/OrchestratorReinject.cs — copy ProcessorReinject and add the D-07
    discriminator + union-field superset (reuse the existing StepOutcome enum — Claude's discretion, choose
    reuse per RESEARCH A5/D-07):

        public sealed record OrchestratorReinject(Guid WorkflowId, Guid StepId, Guid ProcessorId) : IKeeperRecoverable
        {
            public Guid CorrelationId { get; init; }
            public Guid ExecutionId   { get; init; }
            public Guid EntryId       { get; init; }
            public StepOutcome Outcome { get; init; }           // D-07 discriminator
            public string? ErrorMessage { get; init; }          // union — Failed only
            public string? CancellationMessage { get; init; }   // union — Cancelled only
        }

    NO [JsonPropertyName] — default STJ (every record in this folder; init-only props, not positional). Both
    records are `sealed` and implement `IKeeperRecoverable`. Match the exact namespace `Messaging.Contracts`.
  </action>
  <verify>
    <automated>dotnet build src/Messaging.Contracts/Messaging.Contracts.csproj -c Debug 2>&1 | Select-String -Pattern "Build succeeded|error|warning"</automated>
  </verify>
  <acceptance_criteria>
    - File src/Messaging.Contracts/OrchestratorInject.cs exists; grep `record OrchestratorInject` matches; grep `: IKeeperRecoverable` matches in the same file.
    - File src/Messaging.Contracts/OrchestratorReinject.cs exists; grep `record OrchestratorReinject` matches; grep `StepOutcome Outcome` matches; grep `ErrorMessage` and `CancellationMessage` both match.
    - Neither file contains `[JsonPropertyName]`.
    - `dotnet build src/Messaging.Contracts/Messaging.Contracts.csproj` reports Build succeeded, 0 warnings.
  </acceptance_criteria>
  <done>Both contract records compile 0-warning, are sealed, implement IKeeperRecoverable; OrchestratorReinject has the StepOutcome discriminator + ErrorMessage/CancellationMessage union fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add OrchestratorContractTests — round-trip, IKeeperRecoverable, partition-key facts</name>
  <read_first>
    - tests/BaseApi.Tests/Contracts/KeeperContractTests.cs (the analog: round-trip + IKeeperRecoverable assertions for the keeper contracts)
    - tests/BaseApi.Tests/Keeper/RecoveryPartitionFacts.cs (how PartitionGuid is asserted over a contract)
    - src/Keeper/Recovery/ReinjectConsumerDefinition.cs (PartitionGuid SHA256-over-4-tuple — origin-agnostic)
    - src/Messaging.Contracts/OrchestratorInject.cs (the new contract under test)
    - src/Messaging.Contracts/OrchestratorReinject.cs (the new contract under test)
  </read_first>
  <behavior>
    - OrchestratorInject and OrchestratorReinject each implement IKeeperRecoverable (assignability assertion).
    - System.Text.Json round-trip of OrchestratorReinject preserves Outcome + the populated union field (e.g. ErrorMessage on a Failed instance, CancellationMessage on a Cancelled instance).
    - ReinjectConsumerDefinition.PartitionGuid(instance) returns a stable Guid for both new contract types and is identical for two instances sharing the same corr:wf:proc:exec 4-tuple (origin-agnostic — proves the helper needs no change).
  </behavior>
  <action>
    Create tests/BaseApi.Tests/Contracts/OrchestratorContractTests.cs mirroring KeeperContractTests.cs.
    Add facts: (1) `OrchestratorInject_implements_IKeeperRecoverable` and
    `OrchestratorReinject_implements_IKeeperRecoverable` (Assert.IsAssignableFrom<IKeeperRecoverable>);
    (2) `OrchestratorReinject_roundtrips_outcome_and_union_fields` — serialize a Failed instance with
    ErrorMessage and a Cancelled instance with CancellationMessage via JsonSerializer, deserialize, assert
    Outcome + the union field survive; (3) `PartitionGuid_is_stable_and_origin_agnostic` — build an
    OrchestratorInject and an OrchestratorReinject with the SAME corr:wf:proc:exec 4-tuple, assert
    `ReinjectConsumerDefinition.PartitionGuid(a) == ReinjectConsumerDefinition.PartitionGuid(b)` (same key →
    same partition regardless of origin/type). Use xunit.v3 `[Fact]` style consistent with the analog.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj -- --filter-method "*OrchestratorContract*"</automated>
  </verify>
  <acceptance_criteria>
    - File tests/BaseApi.Tests/Contracts/OrchestratorContractTests.cs exists.
    - `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj -- --filter-method "*OrchestratorContract*"` exits 0 with all facts Passed (NOTE: `-- --filter-method`, NOT `--filter` which is silently ignored under MTP).
    - The run reports a small, targeted test count (not ~638) — confirming the filter resolved.
  </acceptance_criteria>
  <done>`*OrchestratorContract*` facts pass: both contracts assert as IKeeperRecoverable, OrchestratorReinject round-trips its Outcome + union field, and PartitionGuid is stable + origin-agnostic across both new types.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | New DTO records on an internal backend bus the orchestrator writes and the keeper reads. No external surface; the values are orchestrator-minted, not user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-03 | Information Disclosure | OrchestratorReinject.ErrorMessage / CancellationMessage carry diagnostic strings on the bus | accept | Same posture as the existing StepFailed.ErrorMessage / StepCancelled.CancellationMessage already on `orchestrator-result` — no new exposure; internal broker only, no external consumer. |
| T-71-04 | (none) | New contracts add NO new key-deletion or key-write capability | accept | The contracts are inert DTOs; deletion authority lives only in KeeperDelete/the pipeline cleanup tail (asserted in Plan 04, D-09). No new threat. |
</threat_model>

<verification>
- `dotnet build src/Messaging.Contracts/Messaging.Contracts.csproj`: Build succeeded, 0 warnings.
- `dotnet test ... -- --filter-method "*OrchestratorContract*"`: all facts Passed.
- grep confirms both records implement IKeeperRecoverable and OrchestratorReinject has the StepOutcome discriminator.
</verification>

<success_criteria>
`OrchestratorInject` and `OrchestratorReinject` exist as sealed `IKeeperRecoverable` records that compile
0-warning, round-trip cleanly under default STJ, and partition through the unchanged 4-tuple helper.
</success_criteria>

<output>
After completion, create `.planning/phases/71-orchestrator-recovery-pipeline/71-02-SUMMARY.md`
</output>
