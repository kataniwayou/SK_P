---
phase: 71-orchestrator-recovery-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Messaging.Contracts/KeeperInject.cs
  - src/Messaging.Contracts/KeeperReinject.cs
  - src/Keeper/Recovery/InjectConsumer.cs
  - src/Keeper/Recovery/ReinjectConsumer.cs
  - src/Keeper/Recovery/RecoveryEndpointBinder.cs
  - src/Keeper/Program.cs
  - src/BaseProcessor.Core/Processing/ProcessorPipeline.cs
  - src/BaseProcessor.Core/Resilience/KeyAbsentException.cs
  - tests/BaseApi.Tests/Keeper/RecoveryTestKit.cs
  - tests/BaseApi.Tests/Contracts/KeeperContractTests.cs
  - tests/BaseApi.Tests/Keeper/InjectConsumerFacts.cs
  - tests/BaseApi.Tests/Keeper/ReinjectConsumerFacts.cs
  - tests/BaseApi.Tests/Keeper/RecoveryPartitionFacts.cs
  - tests/BaseApi.Tests/Keeper/RecoveryDeadLetterFacts.cs
  - tests/BaseApi.Tests/Keeper/KeeperDeleteInvariantFacts.cs
  - tests/BaseApi.Tests/Processor/PipelineForwardFacts.cs
  - tests/BaseApi.Tests/Processor/PipelinePostFacts.cs
  - tests/BaseApi.Tests/Processor/PipelinePreFacts.cs
  - tests/BaseApi.Tests/Processor/PipelineRecoveryFacts.cs
  - tests/BaseApi.Tests/Processor/PipelineEndDeleteFacts.cs
  - tests/BaseApi.Tests/Processor/DispatchTestKit.cs
  - tests/BaseApi.Tests/Resilience/ModelBContractsRetiredFacts.cs
  - tests/BaseApi.Tests/Orchestrator/SC2RecoveryPathsE2ETests.cs
autonomous: true
requirements: [ORCV-06]
must_haves:
  truths:
    - "The solution builds 0-warning after the rename (Release + Debug)"
    - "Zero KeeperInject/KeeperReinject contract-type references remain (excluding the flagged metric-label/Definition false positives)"
    - "The full hermetic test suite is green against the renamed contracts"
    - "RecoveryTestKit.Db() stubs the 5-arg StringSetAsync overload the production 2-arg call binds to (SE.Redis 2.13.1)"
  artifacts:
    - path: "src/Messaging.Contracts/ProcessorInject.cs"
      provides: "Renamed contract record (was KeeperInject)"
      contains: "record ProcessorInject"
    - path: "src/Messaging.Contracts/ProcessorReinject.cs"
      provides: "Renamed contract record (was KeeperReinject)"
      contains: "record ProcessorReinject"
    - path: "src/Keeper/Recovery/ProcessorInjectConsumer.cs"
      provides: "Renamed consumer (was InjectConsumer)"
      contains: "class ProcessorInjectConsumer"
    - path: "src/Keeper/Recovery/ProcessorReinjectConsumer.cs"
      provides: "Renamed consumer (was ReinjectConsumer)"
      contains: "class ProcessorReinjectConsumer"
  key_links:
    - from: "src/Keeper/Recovery/RecoveryEndpointBinder.cs"
      to: "ProcessorInject / ProcessorReinject"
      via: "UsePartitioner<T> / ConfigureConsumer<T>"
      pattern: "UsePartitioner<Processor(Re)?Inject>"
    - from: "src/Keeper/Recovery/ReinjectConsumerDefinition.cs"
      to: "(unchanged)"
      via: "MUST NOT be renamed — origin-agnostic partition helper"
      pattern: "class ReinjectConsumerDefinition"
---

<objective>
Mechanically split the keeper recovery contracts by origin: rename `KeeperInject` → `ProcessorInject`
and `KeeperReinject` → `ProcessorReinject` (file + type), and `InjectConsumer` → `ProcessorInjectConsumer`
and `ReinjectConsumer` → `ProcessorReinjectConsumer` (file + type), updating ALL ~27 `.cs` reference
sites (129 occurrences). `KeeperDelete` / `DeleteConsumer` / `ReinjectConsumerDefinition` stay UNCHANGED.
Fold in the Phase-70 code-review WR-01 fix (D-10): add the missing 5-arg `StringSetAsync` stub to
`RecoveryTestKit.Db()`.

Purpose: This is the dedicated FIRST plan (D-06, user-LOCKED). It is an isolated mechanical diff that
MUST compile green and pass the full suite BEFORE any `Orchestrator*` code lands, so the large rename
reviews cleanly without entanglement with new behavior. Every later plan in this phase copies the
renamed `Processor*` files as its analog.

Output: A green-compiling, green-testing rename. No behavior changes whatsoever.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-orchestrator-recovery-pipeline/71-CONTEXT.md
@.planning/phases/71-orchestrator-recovery-pipeline/71-RESEARCH.md
@.planning/phases/71-orchestrator-recovery-pipeline/71-PATTERNS.md

<rename_blast_radius>
<!-- From 71-RESEARCH.md Q1 — VERIFIED ripgrep counts: 129 occurrences across 27 files. -->
<!-- Rename the LONGER symbol first (ReinjectConsumer before InjectConsumer) or use whole-word -->
<!-- boundaries so ProcessorReinjectConsumer does NOT become ProcessorReProcessorInjectConsumer. -->

KeeperInject (30 refs / 12 files): KeeperInject.cs (RENAME→ProcessorInject.cs), InjectConsumer.cs,
  RecoveryEndpointBinder.cs, ProcessorPipeline.cs (BuildInject return type),
  Contracts/KeeperContractTests.cs, Keeper/InjectConsumerFacts.cs, Keeper/KeeperDeleteInvariantFacts.cs,
  Resilience/ModelBContractsRetiredFacts.cs, Processor/PipelineForwardFacts.cs, Processor/DispatchTestKit.cs,
  Processor/PipelinePostFacts.cs, Orchestrator/SC2RecoveryPathsE2ETests.cs

KeeperReinject (47 refs / 17 files): KeeperReinject.cs (RENAME→ProcessorReinject.cs), ReinjectConsumer.cs,
  RecoveryEndpointBinder.cs, ProcessorPipeline.cs, KeyAbsentException.cs (xml-doc ref),
  ReinjectConsumerFacts.cs, RecoveryPartitionFacts.cs, RecoveryDeadLetterFacts.cs, KeeperDeleteInvariantFacts.cs,
  ModelBContractsRetiredFacts.cs, KeeperContractTests.cs, PipelineRecoveryFacts.cs, PipelinePreFacts.cs,
  PipelineForwardFacts.cs, PipelineEndDeleteFacts.cs, DispatchTestKit.cs, SC2RecoveryPathsE2ETests.cs

InjectConsumer (7 refs / 6 files): RecoveryEndpointBinder.cs, InjectConsumer.cs (RENAME file),
  Keeper/Program.cs, KeeperDeleteInvariantFacts.cs, InjectConsumerFacts.cs, SC2RecoveryPathsE2ETests.cs

ReinjectConsumer (22 refs / 7 files): ReinjectConsumer.cs (RENAME file), RecoveryEndpointBinder.cs,
  Keeper/Program.cs, ReinjectConsumerFacts.cs, RecoveryDeadLetterFacts.cs, KeeperDeleteInvariantFacts.cs,
  SC2RecoveryPathsE2ETests.cs

TRAP 1 (DO NOT RENAME): src/Keeper/Recovery/ReinjectConsumerDefinition.cs — the surviving
  origin-agnostic partition-helper static class (holds PartitionKey/PartitionGuid). It contains the
  substring "ReinjectConsumer" but is NOT a renamed type. Scope the rename to the type names
  InjectConsumer/ReinjectConsumer, NOT ...Definition.

TRAP 2 (INSPECT before touching): AnalyzerE2ETests, PromCounterSnapshot, PassFailEngineFacts matched
  the broad pattern but reference METRIC LABELS (e.g. keeper_reinject_dropped), NOT the C# types.
  Keeper metric names are NOT renamed by D-06. Read each match; leave metric-label strings alone.

DO NOT TOUCH: KeeperDelete.cs, DeleteConsumer.cs, KeeperQueues.cs, IKeeperRecoverable.cs,
  ReinjectConsumerDefinition.cs, L2ProjectionKeys.cs.
</rename_blast_radius>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the two contracts + their two consumers (file + type) and all production reference sites</name>
  <read_first>
    - src/Messaging.Contracts/KeeperInject.cs (the contract to rename → ProcessorInject.cs)
    - src/Messaging.Contracts/KeeperReinject.cs (the contract to rename → ProcessorReinject.cs)
    - src/Keeper/Recovery/InjectConsumer.cs (the consumer to rename → ProcessorInjectConsumer.cs)
    - src/Keeper/Recovery/ReinjectConsumer.cs (the consumer to rename → ProcessorReinjectConsumer.cs)
    - src/Keeper/Recovery/ReinjectConsumerDefinition.cs (TRAP 1 — read to confirm it is NOT renamed)
    - src/Keeper/Recovery/RecoveryEndpointBinder.cs (prod ref site: UsePartitioner/ConfigureConsumer)
    - src/Keeper/Program.cs (prod ref site: AddConsumer registrations)
    - src/BaseProcessor.Core/Processing/ProcessorPipeline.cs (prod ref site: BuildInject/BuildReinject return types ~lines 422-437)
    - src/BaseProcessor.Core/Resilience/KeyAbsentException.cs (prod ref site: xml-doc ref to KeeperReinject)
  </read_first>
  <action>
    Rename the four symbols (and rename the four files to match) using WHOLE-WORD boundaries. Rename the
    LONGER symbols first to avoid the substring trap (`InjectConsumer` is a substring of `ReinjectConsumer`;
    `KeeperInject` is a substring of nothing dangerous but order it consistently):

    1. `KeeperReinject` → `ProcessorReinject`; rename file src/Messaging.Contracts/KeeperReinject.cs → ProcessorReinject.cs
    2. `KeeperInject`   → `ProcessorInject`;   rename file src/Messaging.Contracts/KeeperInject.cs   → ProcessorInject.cs
    3. `ReinjectConsumer` → `ProcessorReinjectConsumer`; rename file src/Keeper/Recovery/ReinjectConsumer.cs → ProcessorReinjectConsumer.cs
    4. `InjectConsumer`   → `ProcessorInjectConsumer`;   rename file src/Keeper/Recovery/InjectConsumer.cs   → ProcessorInjectConsumer.cs

    CRITICAL — TRAP 1: When replacing `ReinjectConsumer`, the static class `ReinjectConsumerDefinition`
    in src/Keeper/Recovery/ReinjectConsumerDefinition.cs MUST NOT change. Use a regex with a trailing
    non-`D`/word boundary, OR explicitly exclude `ReinjectConsumerDefinition` from the replace. After the
    edit, grep-confirm `class ReinjectConsumerDefinition` still exists unchanged.

    Update all PRODUCTION reference sites:
    - src/Keeper/Recovery/RecoveryEndpointBinder.cs: the existing `UsePartitioner<KeeperInject>` /
      `UsePartitioner<KeeperReinject>` and `ConfigureConsumer<InjectConsumer>` / `ConfigureConsumer<ReinjectConsumer>`
      lines become `Processor*`. Leave `ReinjectConsumerDefinition.PartitionGuid` selector calls UNCHANGED.
    - src/Keeper/Program.cs: `AddConsumer<InjectConsumer>()` / `AddConsumer<ReinjectConsumer>()` (with
      `.ExcludeFromConfigureEndpoints()`) become `Processor*`.
    - src/BaseProcessor.Core/Processing/ProcessorPipeline.cs: `BuildInject` returns `ProcessorInject`,
      `BuildReinject` returns `ProcessorReinject` (the return-type names follow the contract rename; the
      BUILDER METHOD names BuildInject/BuildReinject themselves are unchanged unless they embed the keeper
      prefix — they do not).
    - src/BaseProcessor.Core/Resilience/KeyAbsentException.cs: update the xml-doc `<see cref="KeeperReinject"/>`
      reference to `ProcessorReinject`.

    Do NOT touch KeeperDelete, DeleteConsumer, KeeperQueues, IKeeperRecoverable, L2ProjectionKeys, or any
    metric-label string. These keep their `Keeper`/`keeper_` names.
  </action>
  <verify>
    <automated>dotnet build SK_P4.sln -c Debug 2>&1 | Select-String -Pattern "Build succeeded|error|warning"</automated>
  </verify>
  <acceptance_criteria>
    - Files src/Messaging.Contracts/ProcessorInject.cs and ProcessorReinject.cs exist; the old KeeperInject.cs / KeeperReinject.cs no longer exist.
    - Files src/Keeper/Recovery/ProcessorInjectConsumer.cs and ProcessorReinjectConsumer.cs exist; the old InjectConsumer.cs / ReinjectConsumer.cs no longer exist.
    - grep over src/ for `\bKeeperInject\b` and `\bKeeperReinject\b` returns ZERO matches.
    - grep over src/ for `\bInjectConsumer\b`/`\bReinjectConsumer\b` (excluding `ReinjectConsumerDefinition`) returns only the renamed `Processor*` forms.
    - grep `class ReinjectConsumerDefinition` in src/Keeper/Recovery/ReinjectConsumerDefinition.cs still matches (TRAP 1 preserved).
    - `dotnet build SK_P4.sln -c Debug` and `-c Release` both report Build succeeded with 0 warnings, 0 errors.
  </acceptance_criteria>
  <done>Both `-c Debug` and `-c Release` builds succeed 0-warning; the four renamed files exist; ReinjectConsumerDefinition is untouched; no KeeperInject/KeeperReinject type ref remains in src/.</done>
</task>

<task type="auto">
  <name>Task 2: Update all test reference sites to the renamed types, add the D-10 5-arg StringSetAsync stub, and run the full suite green</name>
  <read_first>
    - tests/BaseApi.Tests/Keeper/RecoveryTestKit.cs (D-10 target: Db() at ~lines 58-74 stubs only the 6-arg StringSetAsync)
    - tests/BaseApi.Tests/Contracts/KeeperContractTests.cs (5 KeeperInject + 4 KeeperReinject refs)
    - tests/BaseApi.Tests/Keeper/InjectConsumerFacts.cs
    - tests/BaseApi.Tests/Keeper/ReinjectConsumerFacts.cs
    - tests/BaseApi.Tests/Keeper/RecoveryPartitionFacts.cs
    - tests/BaseApi.Tests/Keeper/RecoveryDeadLetterFacts.cs
    - tests/BaseApi.Tests/Keeper/KeeperDeleteInvariantFacts.cs
    - tests/BaseApi.Tests/Processor/PipelineForwardFacts.cs, PipelinePostFacts.cs, PipelinePreFacts.cs, PipelineRecoveryFacts.cs, PipelineEndDeleteFacts.cs, DispatchTestKit.cs
    - tests/BaseApi.Tests/Resilience/ModelBContractsRetiredFacts.cs
    - tests/BaseApi.Tests/Orchestrator/SC2RecoveryPathsE2ETests.cs
    - .planning/phases/70-processor-inject-cleanup/70-REVIEW.md (WR-01 — the exact 5-arg stub)
  </read_first>
  <action>
    Apply the SAME whole-word rename (`KeeperReinject`→`ProcessorReinject`, `KeeperInject`→`ProcessorInject`,
    `ReinjectConsumer`→`ProcessorReinjectConsumer`, `InjectConsumer`→`ProcessorInjectConsumer`, longer-first,
    TRAP 1 excludes `ReinjectConsumerDefinition`) across every test reference site listed in read_first.
    Where a test FILE name is 1:1 with a renamed type and the convention is to track it (e.g.
    InjectConsumerFacts → may stay as-is; renaming test files is OPTIONAL — only the type references inside
    must update). Keep test file names unless they break a class/file-name lint; the requirement is that the
    renamed TYPE references resolve.

    TRAP 2: For ModelBContractsRetiredFacts.cs and any analyzer/observability-adjacent ref, inspect each
    match: if it is a C# TYPE reference (`KeeperInject`/`KeeperReinject`), rename it; if it is a METRIC
    LABEL string (e.g. `"keeper_reinject_dropped"`), LEAVE IT UNCHANGED. Do not rename metric names.

    D-10 / WR-01 fix in tests/BaseApi.Tests/Keeper/RecoveryTestKit.cs Db(): the kit currently stubs only the
    legacy 6-arg StringSetAsync. SE.Redis 2.13.1 binds the production 2-arg StringSetAsync call to a NEW
    5-arg overload, leaving the stub dead. Add the exact stub from 70-REVIEW WR-01, immediately alongside
    the existing StringSetAsync stub:

        db.StringSetAsync(Arg.Any<RedisKey>(), Arg.Any<RedisValue>(),
            Arg.Any<Expiration>(), Arg.Any<ValueCondition>(), Arg.Any<CommandFlags>()).Returns(true);

    (Add `using` for the StackExchange.Redis types `Expiration`/`ValueCondition` if not already present.)
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj 2>&1 | Select-String -Pattern "Passed!|Failed!|error|Passed:|Failed:"</automated>
  </verify>
  <acceptance_criteria>
    - grep over tests/ for `\bKeeperInject\b` and `\bKeeperReinject\b` returns ZERO type-reference matches (metric-label strings, if any, are not type refs and are excluded).
    - tests/BaseApi.Tests/Keeper/RecoveryTestKit.cs Db() contains a `StringSetAsync(Arg.Any<RedisKey>(), Arg.Any<RedisValue>(), Arg.Any<Expiration>(), Arg.Any<ValueCondition>(), Arg.Any<CommandFlags>())` stub returning true.
    - The FULL suite command `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj` reports `Passed!` (all ~638 tests green, 0 failed). NOTE: this is the full-suite wave gate — do NOT use `--filter` (silently ignored under MTP); the rename must compile + pass everything.
  </acceptance_criteria>
  <done>Full suite green (Passed!, 0 failed); no KeeperInject/KeeperReinject type ref remains in tests/; the 5-arg StringSetAsync stub is present in RecoveryTestKit.Db().</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is a pure mechanical rename + a test-stub fix. No new code path, no new input surface, no new trust boundary crosses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01 | Tampering | MassTransit message-type URN change (KeeperInject→ProcessorInject on the bus) | accept | In-flight old-type messages on `keeper-recovery` at deploy would not bind to the renamed consumer (RESEARCH Runtime State A2). Mitigation is operational, not code: deploy on a DRAINED `keeper-recovery` queue per the project close-gate net-zero protocol. No code change; same-deploy contract+consumer rename has no intra-deploy version skew. |
| T-71-02 | (none) | Rename of types only | accept | No new threat introduced — no new external surface, no new key, no new deletion path. The delete invariant is unchanged (KeeperDelete/DeleteConsumer untouched). |
</threat_model>

<verification>
- `dotnet build SK_P4.sln -c Debug` and `-c Release`: Build succeeded, 0 warnings, 0 errors.
- `dotnet test tests/BaseApi.Tests/BaseApi.Tests.csproj`: Passed!, 0 failed (full suite — the rename wave gate).
- grep src/ + tests/ for `\bKeeperInject\b`/`\bKeeperReinject\b`: zero type-ref matches.
- `class ReinjectConsumerDefinition` still present and unchanged.
</verification>

<success_criteria>
The rename compiles green (Debug + Release, 0-warning) and the full hermetic suite passes BEFORE any
`Orchestrator*` code exists. `ProcessorInject`/`ProcessorReinject`/`ProcessorInjectConsumer`/
`ProcessorReinjectConsumer` are the live names; `KeeperDelete`/`DeleteConsumer`/`ReinjectConsumerDefinition`
are untouched; the D-10 5-arg `StringSetAsync` stub is in place.
</success_criteria>

<output>
After completion, create `.planning/phases/71-orchestrator-recovery-pipeline/71-01-SUMMARY.md`
</output>
