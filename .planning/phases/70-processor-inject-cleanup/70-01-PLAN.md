---
phase: 70-processor-inject-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Keeper/Recovery/InjectConsumer.cs
  - src/Messaging.Contracts/KeeperInject.cs
  - src/BaseProcessor.Core/Processing/ProcessorPipeline.cs
autonomous: true
requirements: [KINJ-01, KINJ-02]

must_haves:
  truths:
    - "InjectConsumer.HandleAsync performs exactly two effects — write L2[EntryId]=Data then send StepCompleted to queue:orchestrator-result — and issues no KeyDelete* (the former source-delete tail at InjectConsumer.cs:40 is gone)."
    - "KeeperInject no longer carries DeleteEntryId; the field is deleted from the contract record."
    - "ProcessorPipeline.BuildInject constructs KeeperInject without DeleteEntryId."
    - "A source/reflection scan finds no remaining DeleteEntryId reference on the INJECT path (grep returns zero code hits)."
  artifacts:
    - path: "src/Keeper/Recovery/InjectConsumer.cs"
      provides: "Two-effect non-destructive INJECT consumer body (write data + send StepCompleted)"
      contains: "StringSetAsync"
    - path: "src/Messaging.Contracts/KeeperInject.cs"
      provides: "Reduced KeeperInject record (5-id base + EntryId + Data, no DeleteEntryId)"
      contains: "public string Data"
    - path: "src/BaseProcessor.Core/Processing/ProcessorPipeline.cs"
      provides: "BuildInject minting KeeperInject without DeleteEntryId"
      contains: "new KeeperInject"
  key_links:
    - from: "src/BaseProcessor.Core/Processing/ProcessorPipeline.cs (BuildInject)"
      to: "src/Messaging.Contracts/KeeperInject.cs"
      via: "constructs the reduced KeeperInject record (no DeleteEntryId initializer)"
      pattern: "new KeeperInject"
    - from: "src/Keeper/Recovery/InjectConsumer.cs (HandleAsync)"
      to: "queue:orchestrator-result"
      via: "Guard(GetSendEndpoint) + Guard(ep.Send) of StepCompleted — and NO KeyDeleteAsync"
      pattern: "GetSendEndpoint"
---

<objective>
Make the keeper INJECT path non-destructive in production code: remove the trailing
source-delete from `InjectConsumer.HandleAsync` (D-01), drop the now-vestigial
`KeeperInject.DeleteEntryId` field from the contract (D-02), and stop supplying it in
`ProcessorPipeline.BuildInject` (D-03). After this plan the INJECT path matches the
canonical recovery spec (§8: DELETE is the only state that deletes) and the
already-non-destructive REINJECT path.

Purpose: KINJ-01 (non-destructive INJECT) + the KINJ-02 source half (field removed from
contract + producer). This is the prerequisite contract reshape — Plan 02's test edits
assert against the reduced shape and will not compile until this plan ships.

Output: Three edited source files. After this plan `grep -r DeleteEntryId src/` returns
zero hits (all remaining hits are in `tests/`, reshaped by Plan 02).

No wire migration / versioning step: `KeeperInject` is a default-STJ bus envelope (no
`[JsonPropertyName]`). STJ ignores unknown properties on deserialize, so an in-flight
pre-deploy `KeeperInject` carrying `DeleteEntryId` deserializes harmlessly after deploy
(D-03 note). Do NOT add a version/migration step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/70-processor-inject-cleanup/70-CONTEXT.md
@.planning/phases/70-processor-inject-cleanup/70-RESEARCH.md
@.planning/phases/70-processor-inject-cleanup/70-PATTERNS.md
@docs/design/processor-keeper-recovery-spec.md

<interfaces>
<!-- The two surviving Guarded effects in InjectConsumer (preserve verbatim, RESEARCH §"INJECT consumer body (after D-01)"): -->
```csharp
// op 1 (KEEP): write L2[entryId] = data (data in-hand — forward-only, NO presence read)
await Guard(() => Db.StringSetAsync(L2ProjectionKeys.ExecutionData(m.EntryId), m.Data), ct);

// op 2 (KEEP): send StepCompleted → orchestrator result queue (A15)
var completed = new StepCompleted(m.WorkflowId, m.StepId, m.ProcessorId)
{
    CorrelationId = m.CorrelationId, ExecutionId = m.ExecutionId, EntryId = m.EntryId,
};
var ep = await Guard(() => Send.GetSendEndpoint(new Uri($"queue:{OrchestratorQueues.Result}")), ct);
await Guard(() => ep.Send(completed, CancellationToken.None), ct);
```
<!-- The op-3 block to REMOVE (comment :39 + op :40 together): -->
```csharp
// 3) delete L2[deleteEntryId] (source cleanup tail — AFTER the confirmed send)
await Guard(() => Db.KeyDeleteAsync(L2ProjectionKeys.ExecutionData(m.DeleteEntryId)), ct);
```
<!-- KeeperInject after D-02: 5-id base (WorkflowId, StepId, ProcessorId + CorrelationId/ExecutionId) + EntryId (Guid, :12) + Data (string, :13). Drop the DeleteEntryId property (:14) and its XML-doc mention (:4-7). KEEP the ":3 NO [JsonPropertyName]" wire-tolerance comment. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the source-delete op from InjectConsumer (D-01)</name>
  <files>src/Keeper/Recovery/InjectConsumer.cs</files>
  <read_first>
    - src/Keeper/Recovery/InjectConsumer.cs (whole file — preserve ops 1-2's Guard wrapping)
    - 70-PATTERNS.md §"InjectConsumer.cs (EDIT) [D-01]" (lines 102-115 — exact block to delete + doc-rewrite guidance)
    - 70-RESEARCH.md §"The INJECT consumer body (after D-01)" (lines 112-130) and Pitfall verification row (lines 19-20)
    - src/Keeper/Recovery/ReinjectConsumer.cs (the already-non-destructive sibling reference — do NOT edit it)
  </read_first>
  <action>
    In `src/Keeper/Recovery/InjectConsumer.cs`:
    1. DELETE the op-3 block — both the comment (`:39`) and the op (`:40`) together:
       ```csharp
       // 3) delete L2[deleteEntryId] (source cleanup tail — AFTER the confirmed send)
       await Guard(() => Db.KeyDeleteAsync(L2ProjectionKeys.ExecutionData(m.DeleteEntryId)), ct);
       ```
       Leave ops 1-2 (`:24-37`: the `Guard(() => Db.StringSetAsync(...))` write and the
       `StepCompleted` build + `Guard(GetSendEndpoint)` + `Guard(ep.Send)`) byte-for-byte
       intact — the Guarded structure of ops 1-2 must not be disturbed (it is the
       established RecoveryConsumerBase pattern).
    2. REWRITE the class XML doc (`:10-16`): drop the "three ops in STRICT order", the
       Pitfall-5 "source delete is the tail AFTER the confirmed send" rationale, and the
       "(3) delete `L2[m.DeleteEntryId]`" clause. New doc describes the two-effect
       non-destructive body: write the data key + send `StepCompleted`. KEEP the sentence
       about every op going through the RetryLoop `Guard` and endpoint gating (D-04).
    Result: `HandleAsync` has exactly two Guarded effects; no `KeyDelete` substring remains
    in the file. Note the observed §8 index-slot divergence is DEFERRED — do NOT add an
    index-slot write here.
  </action>
  <verify>
    <automated>dotnet build src/Keeper -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `src/Keeper/Recovery/InjectConsumer.cs` contains NO `KeyDelete` substring.
    - `src/Keeper/Recovery/InjectConsumer.cs` contains NO `DeleteEntryId` substring.
    - The file still contains `StringSetAsync` (op 1) and `GetSendEndpoint` (op 2).
    - `grep -n "KeyDelete" src/Keeper/Recovery/InjectConsumer.cs` returns zero hits.
    - `src/Keeper/Recovery/ReinjectConsumer.cs` is unchanged (not touched).
  </acceptance_criteria>
  <done>InjectConsumer.HandleAsync performs exactly two Guarded effects (write data + send StepCompleted) and issues no KeyDelete*; the XML doc describes the non-destructive two-effect body.</done>
</task>

<task type="auto">
  <name>Task 2: Drop DeleteEntryId from the KeeperInject contract (D-02)</name>
  <files>src/Messaging.Contracts/KeeperInject.cs</files>
  <read_first>
    - src/Messaging.Contracts/KeeperInject.cs (whole file — small record; KEEP the ":3 NO [JsonPropertyName]" wire-tolerance comment)
    - 70-PATTERNS.md §"KeeperInject.cs (EDIT) [D-02]" (lines 118-129 — exact property + doc clause to drop)
    - 70-RESEARCH.md verification rows (lines 21-22) and §"Wire-tolerance" / Runtime State Inventory (lines 176, 219-220)
  </read_first>
  <action>
    In `src/Messaging.Contracts/KeeperInject.cs`:
    1. DELETE the property at `:14`:
       ```csharp
       public Guid DeleteEntryId { get; init; }   // (its trailing comment too)
       ```
    2. REWRITE the record XML doc (`:4-7`): drop the
       `<see cref="DeleteEntryId"/> (source entryId deleted after the send)` clause from
       the id-set sentence → "INJECT id-set: `EntryId` (allocation to write) + `Data`
       (raw-JSON output, in-hand)".
    The record then carries the 5-id base (ctor `WorkflowId, StepId, ProcessorId` +
    `CorrelationId`/`ExecutionId`) + `EntryId` (Guid, `:12`) + `Data` (string, `:13`).
    KEEP the `:3` "bus envelope — NO [JsonPropertyName], default STJ" comment verbatim —
    it is the wire-tolerance rationale. Do NOT add any `[JsonPropertyName]`, version field,
    or migration shim (D-03 note: STJ drops the unknown property on in-flight messages).
  </action>
  <verify>
    <automated>dotnet build src/Messaging.Contracts -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `src/Messaging.Contracts/KeeperInject.cs` contains NO `DeleteEntryId` substring.
    - The file still contains `public string Data` and `public Guid EntryId`.
    - The file still contains the "NO [JsonPropertyName]" / default-STJ comment.
    - `grep -n "DeleteEntryId" src/Messaging.Contracts/KeeperInject.cs` returns zero hits.
  </acceptance_criteria>
  <done>KeeperInject carries only the 5-id base + EntryId + Data; DeleteEntryId is gone from the record and its XML doc.</done>
</task>

<task type="auto">
  <name>Task 3: Stop supplying DeleteEntryId in ProcessorPipeline.BuildInject (D-03)</name>
  <files>src/BaseProcessor.Core/Processing/ProcessorPipeline.cs</files>
  <read_first>
    - src/BaseProcessor.Core/Processing/ProcessorPipeline.cs — BuildInject (`:430-438`) + the INFRA-02/Pitfall-1 comment (`:428-429`); sibling builders BuildReinject (`:422-423`), BuildDelete (`:425-426`) for the initializer-pattern reference
    - 70-PATTERNS.md §"ProcessorPipeline.cs BuildInject (EDIT) [D-03]" (lines 132-142 — exact line + comment to drop)
    - 70-RESEARCH.md verification row (line 23)
  </read_first>
  <action>
    In `src/BaseProcessor.Core/Processing/ProcessorPipeline.cs`, `BuildInject` (`:430-438`):
    1. DELETE the initializer line `:437`:
       ```csharp
       DeleteEntryId = d.EntryId,         // source entryId (A18 literal deleteEntryId)
       ```
       The remaining `new KeeperInject(d.WorkflowId, d.StepId, d.ProcessorId) { ... }`
       initializer keeps `EntryId = the allocation just written` and `Data = the raw-JSON
       output in-hand` (same shape as sibling BuildReinject/BuildDelete, minus one line).
    2. REWRITE the INFRA-02/Pitfall-1 comment (`:428-429`): drop the
       `, DeleteEntryId = the source entryId` clause → keep "BuildInject populates
       EntryId = the allocation just written, Data = the raw-JSON output in-hand."
    Do NOT alter BuildReinject or BuildDelete.
  </action>
  <verify>
    <automated>dotnet build src/BaseProcessor.Core -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `src/BaseProcessor.Core/Processing/ProcessorPipeline.cs` contains NO `DeleteEntryId` substring.
    - The file still contains `new KeeperInject` in BuildInject.
    - `grep -rn "DeleteEntryId" src/` returns ZERO hits across the entire `src/` tree (all three source touch-points clean).
  </acceptance_criteria>
  <done>BuildInject constructs KeeperInject without DeleteEntryId; the whole src/ tree is free of DeleteEntryId. (Test references in tests/ are reshaped by Plan 02 — the solution will not fully build until then; src/ projects build clean on their own.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keeper-recovery bus → InjectConsumer | A `KeeperInject` envelope (internal, processor-minted) crosses into the recovery worker. No external/untrusted input — the producer is `ProcessorPipeline.BuildInject` in the same trust domain. |
| InjectConsumer → Redis L2 / orchestrator-result queue | The consumer writes a data key and sends `StepCompleted`. This plan REMOVES a write (the source-delete), never adds one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | Tampering / Repudiation (data loss) | InjectConsumer source-delete removal | accept (risk reduced) | Removing the destructive `KeyDeleteAsync` strictly narrows the data-loss window the historical Pitfall-5 "delete-after-send" ordering existed to defend. The cleanup of stale source keys is now owned solely by the out-of-band atomic two-key DELETE state (DeleteConsumer) on exhaust, per spec §8; Phase 69 already made the processor skip its cleanup tail on escalation (§4.3), so no key is orphaned by this change. Net security effect non-negative — no mitigation needed. |
| T-70-02 | Tampering | KeeperInject.DeleteEntryId field drop (wire) | accept | `KeeperInject` is an internal default-STJ bus envelope; dropping a field REDUCES the deserialized surface. STJ unknown-property tolerance (the existing posture) means an in-flight pre-deploy envelope carrying `DeleteEntryId` deserializes harmlessly post-deploy. No new auth/input/secret surface. |

**Conclusion:** No high-severity threats. This plan removes one destructive Redis op and narrows one wire field — both strictly non-negative for security (ASVS V2/V3/V4/V5/V6 surfaces unchanged; see 70-RESEARCH.md §Security Domain). No mitigations are invented for non-threats per the security-enforcement guidance.
</threat_model>

<verification>
- `grep -rn "DeleteEntryId" src/` → zero hits (all three source touch-points clean).
- `grep -n "KeyDelete" src/Keeper/Recovery/InjectConsumer.cs` → zero hits.
- `dotnet build src/Keeper -c Debug`, `dotnet build src/Messaging.Contracts -c Debug`,
  `dotnet build src/BaseProcessor.Core -c Debug` each succeed.
- (Full-solution build + 0-warning Release/Debug gate is asserted in Plan 02 after the
  test references are reshaped — the solution cannot fully compile mid-wave because
  tests/ still reference the dropped field until Plan 02 runs.)
</verification>

<success_criteria>
- InjectConsumer.HandleAsync = exactly two Guarded effects (write data + send StepCompleted), no KeyDelete* (KINJ-01).
- KeeperInject has no DeleteEntryId; BuildInject omits it; `src/` grep for DeleteEntryId is empty (KINJ-02 source half).
- The three src/ projects build clean in isolation; no wire-version/migration step was added.
</success_criteria>

<output>
After completion, create `.planning/phases/70-processor-inject-cleanup/70-01-SUMMARY.md`.
</output>
