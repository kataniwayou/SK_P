---
phase: 70-processor-inject-cleanup
plan: 02
type: execute
wave: 2
depends_on: [70-01]
files_modified:
  - tests/BaseApi.Tests/Keeper/KeeperDeleteInvariantFacts.cs
  - tests/BaseApi.Tests/Keeper/InjectConsumerFacts.cs
  - tests/BaseApi.Tests/Contracts/KeeperContractTests.cs
  - tests/BaseApi.Tests/Processor/PipelineForwardFacts.cs
  - tests/BaseApi.Tests/Orchestrator/SC2RecoveryPathsE2ETests.cs
autonomous: true
requirements: [KINJ-02, KINJ-03]

must_haves:
  truths:
    - "A negative-guard fact proves DELETE is the only keeper state that deletes keys — DeleteConsumer DOES delete (multi-key RedisKey[] overload); InjectConsumer and ReinjectConsumer do NOT (both KeyDeleteAsync overloads), each co-asserted with a positive side-effect so a no-op consumer cannot pass."
    - "KeeperContractTests reflection fact asserts EntryId+Data present AND Assert.Null(GetProperty(\"DeleteEntryId\")) so re-adding the field breaks the build."
    - "InjectConsumerFacts, PipelineForwardFacts, and SC2RecoveryPathsE2ETests are updated to the reduced id-set and stay green (E2E compiles; it is RealStack/excluded from the hermetic gate)."
    - "The full BaseApi.Tests suite is green and the solution builds 0-warning in both Release and Debug (D-10)."
  artifacts:
    - path: "tests/BaseApi.Tests/Keeper/KeeperDeleteInvariantFacts.cs"
      provides: "The KINJ-03 cross-consumer delete-invariant fact (DeleteConsumer deletes; Inject/Reinject do not)"
      contains: "DidNotReceive"
      min_lines: 60
    - path: "tests/BaseApi.Tests/Contracts/KeeperContractTests.cs"
      provides: "Reflection guard: EntryId+Data present, DeleteEntryId Assert.Null"
      contains: "Assert.Null(typeof(KeeperInject).GetProperty(\"DeleteEntryId\"))"
    - path: "tests/BaseApi.Tests/Keeper/InjectConsumerFacts.cs"
      provides: "Reshaped INJECT fact: write-then-send, DidNotReceive delete belt"
      contains: "DidNotReceive"
    - path: "tests/BaseApi.Tests/Processor/PipelineForwardFacts.cs"
      provides: "NODROP-01 fact with the inj.DeleteEntryId assertion removed"
    - path: "tests/BaseApi.Tests/Orchestrator/SC2RecoveryPathsE2ETests.cs"
      provides: "E2E asserting INJECT writes data + sends StepCompleted and deletes nothing (compile-only)"
  key_links:
    - from: "tests/BaseApi.Tests/Keeper/KeeperDeleteInvariantFacts.cs"
      to: "InjectConsumer / ReinjectConsumer / DeleteConsumer (behavioral NSubstitute)"
      via: "Consume against RecoveryTestKit.Db() substitute, assert Received/DidNotReceive KeyDeleteAsync"
      pattern: "KeyDeleteAsync"
    - from: "tests/BaseApi.Tests/Contracts/KeeperContractTests.cs"
      to: "Messaging.Contracts.KeeperInject"
      via: "reflection GetProperty negative guard"
      pattern: "GetProperty\\(\"DeleteEntryId\"\\)"
---

<objective>
Reshape the tests to the reduced `KeeperInject` shape and lock the delete invariant.
Invert the delete assertions across four existing fact files (D-06, D-07, D-08, D-09) and
add ONE dedicated invariant fact (D-04/D-05) that makes "DELETE is the only keeper state
that deletes keys" a build-breaking, behaviorally-enforced guarantee.

Purpose: the KINJ-02 test half (reflection field-removal guard + golden/Phase-50 test
updates + 0-warning dual-config build gate) and KINJ-03 (the negative-guard invariant
fact). Depends on Plan 01 — `DeleteEntryId` is gone from the contract, so every test that
still references it must be reshaped here for the solution to compile.

Output: 4 edited test files + 1 net-new fact file (`KeeperDeleteInvariantFacts.cs`,
Wave-0 gap from 70-VALIDATION.md). Full `BaseApi.Tests` suite green; 0-warning Release +
Debug build.

Guard style is behavioral NSubstitute (D-04) — run the REAL `HandleAsync` against a
`RecoveryTestKit.Db()` substitute and assert on the calls it made. Do NOT introduce a
reflection/IL/source-text "no KeyDelete anywhere" scan, and never instantiate or assert
against `L2ProbeRecovery` (its `:35` scratch self-delete is the explicit carve-out).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/70-processor-inject-cleanup/70-CONTEXT.md
@.planning/phases/70-processor-inject-cleanup/70-RESEARCH.md
@.planning/phases/70-processor-inject-cleanup/70-PATTERNS.md
@.planning/phases/70-processor-inject-cleanup/70-VALIDATION.md
@.planning/phases/70-processor-inject-cleanup/70-01-SUMMARY.md

<interfaces>
<!-- Shared harness: tests/BaseApi.Tests/Keeper/RecoveryTestKit.cs -->
<!-- Db() :58 — substitute IDatabase; stubs BOTH delete overloads (:71 single RedisKey, :72 RedisKey[]→2L) so Received/DidNotReceive work for both. -->
<!-- Mux(db) :49 · Retry() :35 · Metrics() :41 (ReinjectConsumer arm only) · CapturingSendProvider :79 (records (Uri, Message); captures StepCompleted :88 + EntryStepDispatch :86) -->

<!-- POSITIVE assertion — DELETE deletes the multi-key (RedisKey[]) overload — copy from DeleteConsumerFacts.cs:51-55 (DeleteConsumer.cs:20 uses RedisKey[]): -->
```csharp
await db.Received(1).KeyDeleteAsync(
    Arg.Is<RedisKey[]>(ks => ks.Length == 2
        && ks.Contains((RedisKey)L2ProjectionKeys.ExecutionData(m.EntryId))
        && ks.Contains((RedisKey)L2ProjectionKeys.MessageIndex(m.MessageId))),
    Arg.Any<CommandFlags>());
```

<!-- NEGATIVE assertion — INJECT / REINJECT do NOT delete — BOTH overloads (Pitfall 2): -->
```csharp
await db.DidNotReceive().KeyDeleteAsync(Arg.Any<RedisKey>(),   Arg.Any<CommandFlags>());  // single-key
await db.DidNotReceive().KeyDeleteAsync(Arg.Any<RedisKey[]>(), Arg.Any<CommandFlags>());  // multi-key
```

<!-- ReinjectConsumer (5-arg ctor; HandleAsync drops on STRLEN==0 — stub present so the send path runs) — copy ReinjectConsumerFacts.cs:44-51: -->
```csharp
db.StringLengthAsync(L2ProjectionKeys.ExecutionData(m.EntryId), Arg.Any<CommandFlags>()).Returns(10L); // present
var consumer = new ReinjectConsumer(
    RecoveryTestKit.Mux(db), send, RecoveryTestKit.Retry(),
    RecoveryTestKit.Metrics(), NullLogger<ReinjectConsumer>.Instance);
```

<!-- SE.Redis 2.13.1 overload quirk (Pitfall 1) — ONLY if a fact also asserts the write; use the 5-arg shape: -->
```csharp
await db.Received(1).StringSetAsync(
    (RedisKey)L2ProjectionKeys.ExecutionData(m.EntryId), (RedisValue)m.Data,
    Arg.Any<Expiration>(), Arg.Any<ValueCondition>(), Arg.Any<CommandFlags>());
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the dedicated delete-invariant fact (D-04 / D-05) [KINJ-03]</name>
  <files>tests/BaseApi.Tests/Keeper/KeeperDeleteInvariantFacts.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Keeper/RecoveryTestKit.cs (Db() :58, Mux :49, Retry :35, Metrics :41, CapturingSendProvider :79; both delete-overload stubs :71-72)
    - tests/BaseApi.Tests/Keeper/DeleteConsumerFacts.cs (imports :1-7; Ctx idiom :17-23; ctor :42-44; positive RedisKey[] Received :51-55)
    - tests/BaseApi.Tests/Keeper/InjectConsumerFacts.cs (3-arg ctor :25-27; captured-send assert :44-46)
    - tests/BaseApi.Tests/Keeper/ReinjectConsumerFacts.cs (5-arg ctor :48-51; STRLEN-present stub :44-45; captured EntryStepDispatch :55-57)
    - src/Keeper/Recovery/DeleteConsumer.cs:20-24 (the RedisKey[] two-key delete — data key + message index)
    - 70-PATTERNS.md §"KeeperDeleteInvariantFacts.cs (NEW) — PRIMARY MAPPING" (lines 26-98)
    - 70-RESEARCH.md §"Validation Architecture" KINJ-03 row (line 238) + Pitfalls 2 & 3 (lines 194-203)
  </read_first>
  <action>
    Create `tests/BaseApi.Tests/Keeper/KeeperDeleteInvariantFacts.cs`, namespace
    `BaseApi.Tests.Keeper`, class `KeeperDeleteInvariantFacts`, `[Trait("Phase","70")]`,
    with THREE `[Fact]`s. Reuse `RecoveryTestKit` (do NOT hand-roll fakes). Imports per
    PATTERNS lines 35-44 (note `global::Keeper.Recovery` alias; the Reinject arm also needs
    `global::Keeper.Observability` for `KeeperMetrics` and
    `Microsoft.Extensions.Logging.Abstractions` for `NullLogger<ReinjectConsumer>`). Use
    `TestContext.Current.CancellationToken` for `ct`.

    1. `DeleteConsumer_deletes_both_keys` — build a `KeeperDelete` message, construct
       `new DeleteConsumer(RecoveryTestKit.Mux(db), send, RecoveryTestKit.Retry())`, fake
       `ConsumeContext<KeeperDelete>` (Ctx idiom), `await consumer.Consume(ctx)`. Assert the
       POSITIVE multi-key delete (the `RedisKey[]` overload, `ks.Length == 2` containing
       `ExecutionData(m.EntryId)` + `MessageIndex(m.MessageId)`) — copy the matcher shape
       from the `<interfaces>` block / DeleteConsumerFacts.cs:51-55.

    2. `InjectConsumer_never_deletes` — build a `KeeperInject` (reduced shape: EntryId +
       Data, NO DeleteEntryId — it no longer exists after Plan 01), construct
       `new InjectConsumer(RecoveryTestKit.Mux(db), send, RecoveryTestKit.Retry())`, Consume.
       Assert NEGATIVE on BOTH overloads (`DidNotReceive().KeyDeleteAsync(Arg.Any<RedisKey>(), ...)`
       AND `Arg.Any<RedisKey[]>()`). CO-ASSERT the POSITIVE side-effect so a no-op can't pass:
       `var (_, msg) = Assert.Single(send.Sent); Assert.IsType<StepCompleted>(msg);`.

    3. `ReinjectConsumer_never_deletes` — STUB STRLEN present first
       (`db.StringLengthAsync(L2ProjectionKeys.ExecutionData(m.EntryId), Arg.Any<CommandFlags>()).Returns(10L);`
       — ReinjectConsumer drops on STRLEN==0, so this is required to exercise its send path),
       construct the 5-arg `new ReinjectConsumer(Mux, send, Retry(), RecoveryTestKit.Metrics(),
       NullLogger<ReinjectConsumer>.Instance)`, Consume a `KeeperReinject`. Assert NEGATIVE on
       BOTH overloads. CO-ASSERT the POSITIVE side-effect: `Assert.Single(send.Sent)` (the
       captured `EntryStepDispatch`).

    CARVE-OUT (Pitfall 3): construct ONLY DeleteConsumer, InjectConsumer, ReinjectConsumer.
    NEVER reference/instantiate `L2ProbeRecovery` and never enumerate `Keeper.dll` types —
    behavioral scoping is what keeps the probe's scratch `:35` delete out of the invariant.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests -- --filter-method "*KeeperDeleteInvariant*"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/BaseApi.Tests/Keeper/KeeperDeleteInvariantFacts.cs` exists with three `[Fact]` methods.
    - File contains `DidNotReceive` (negative guard) and `Received(1)` (positive DELETE).
    - File contains `Arg.Any<RedisKey[]>()` AND `Arg.Any<RedisKey>()` (both overloads asserted).
    - File contains NO `L2ProbeRecovery` substring and NO `Keeper.dll` / type-enumeration code.
    - `dotnet test tests/BaseApi.Tests -- --filter-method "*KeeperDeleteInvariant*"` exits 0 (3 facts pass).
  </acceptance_criteria>
  <done>The invariant fact proves DELETE deletes (multi-key) and INJECT + REINJECT do not (both overloads), each with a positive side-effect co-assertion; L2ProbeRecovery is never instantiated.</done>
</task>

<task type="auto">
  <name>Task 2: Reshape InjectConsumerFacts (D-07) + KeeperContractTests reflection guard (D-06)</name>
  <files>tests/BaseApi.Tests/Keeper/InjectConsumerFacts.cs, tests/BaseApi.Tests/Contracts/KeeperContractTests.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Keeper/InjectConsumerFacts.cs (whole file — message build :22-39 incl. :35; InOrder chain :57-63; belt :68; KeyDelete Received :73-74; write Received 5-arg :70-72; captured-send :44-49; doc :11-15; method name :20)
    - tests/BaseApi.Tests/Contracts/KeeperContractTests.cs (INJECT fact :66-81; method name :67; class doc :12-13; deleteEntryId positive block :78-80; sibling Assert.Null pattern :37; sibling facts :50-64)
    - 70-PATTERNS.md §"InjectConsumerFacts.cs (EDIT) [D-07]" (lines 145-154) + §"KeeperContractTests.cs (EDIT) [D-06]" (lines 158-169)
    - 70-RESEARCH.md Pitfall 1 (lines 182-192) + Pitfall 5 (lines 210-213)
  </read_first>
  <action>
    A) `tests/BaseApi.Tests/Keeper/InjectConsumerFacts.cs` (D-07):
    1. Remove `DeleteEntryId = Guid.NewGuid(),` from the test `KeeperInject` message (`:35`).
    2. DROP the `Received.InOrder(...)` chain (`:57-63`) — with the delete gone the db
       substitute sees only one call, so a multi-element InOrder no longer guards anything
       (Pitfall 5). DROP the positive delete assertion (`:73-74`
       `db.Received(1).KeyDeleteAsync((RedisKey)…ExecutionData(m.DeleteEntryId), …)`).
    3. KEEP / express the surviving order as write-then-send (Claude's discretion, D-07):
       (a) the 5-arg `db.Received(1).StringSetAsync(...)` shape (`:70-72`, Pitfall 1 — use the
       `Expiration`/`ValueCondition` overload exactly) AND (b) `Assert.Single(send.Sent)` is the
       `StepCompleted` (`:44-49`). The write-before-send ordering is implicit in the consumer body.
    4. ADD the belt — `db.DidNotReceive().KeyDeleteAsync(...)` on BOTH overloads
       (`Arg.Any<RedisKey>()` and `Arg.Any<RedisKey[]>()`).
    5. Rewrite the class XML doc (`:11-15`) and rename the method (`:20`
       `Inject_writes_sends_completed_deletes_source_in_order` → e.g.
       `Inject_writes_then_sends_completed_and_deletes_nothing`).

    B) `tests/BaseApi.Tests/Contracts/KeeperContractTests.cs` (D-06), INJECT fact (`:66-81`):
    1. Rename `KeeperInject_carries_the_A18_id_set_EntryId_Data_DeleteEntryId` →
       `KeeperInject_carries_the_reduced_id_set_EntryId_Data`.
    2. KEEP the `EntryId` (Guid) + `Data` (string) positive `GetProperty`/`Assert.NotNull`
       asserts (`:70-76`).
    3. REPLACE the `deleteEntryId` positive block (`:78-80`) with the negative guard:
       `Assert.Null(typeof(KeeperInject).GetProperty("DeleteEntryId"));`
    4. Update the class XML doc (`:12-13`): "INJECT carries … + DeleteEntryId" → "INJECT
       carries EntryId + Data". LEAVE the REINJECT (EntryId+Payload) and DELETE
       (EntryId+MessageId) facts and their doc sentences unchanged.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests -- --filter-method "*InjectConsumerFacts*|*KeeperContractTests*"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "DeleteEntryId" tests/BaseApi.Tests/Keeper/InjectConsumerFacts.cs` returns zero hits.
    - `InjectConsumerFacts.cs` contains `DidNotReceive` and a 5-arg `StringSetAsync` Received check; contains NO `Received.InOrder`.
    - `KeeperContractTests.cs` contains `Assert.Null(typeof(KeeperInject).GetProperty("DeleteEntryId"))`.
    - `KeeperContractTests.cs` no longer contains a method named `*A18_id_set*DeleteEntryId*`.
    - `dotnet test tests/BaseApi.Tests -- --filter-method "*InjectConsumerFacts*|*KeeperContractTests*"` exits 0.
  </acceptance_criteria>
  <done>InjectConsumerFacts asserts write-then-send + DidNotReceive-delete (both overloads); KeeperContractTests asserts EntryId+Data present and DeleteEntryId Assert.Null (re-adding the field breaks the build).</done>
</task>

<task type="auto">
  <name>Task 3: Reshape PipelineForwardFacts (D-08) + SC2 E2E delete-half (D-09) + dual-config build gate (D-10)</name>
  <files>tests/BaseApi.Tests/Processor/PipelineForwardFacts.cs, tests/BaseApi.Tests/Orchestrator/SC2RecoveryPathsE2ETests.cs</files>
  <read_first>
    - tests/BaseApi.Tests/Processor/PipelineForwardFacts.cs (NODROP-01 doc :19; KeeperInject capture :147; inj.Data/inj.EntryId asserts :148,:150; the assertion to drop :149)
    - tests/BaseApi.Tests/Orchestrator/SC2RecoveryPathsE2ETests.cs (doc :40-43, :178-179; the INJECT delete-half block :188-215 — full footprint; the write assertion to KEEP :209-211; entryKey cleanup :195)
    - 70-PATTERNS.md §"PipelineForwardFacts.cs (EDIT) [D-08]" (lines 173-179) + §"SC2RecoveryPathsE2ETests.cs (EDIT) [D-09]" (lines 183-197)
    - 70-RESEARCH.md Pitfall 4 (lines 205-208 — the FULL block, not 3 lines) + Pitfall 6 (lines 215-216) + Assumptions A1 (line 299)
  </read_first>
  <action>
    A) `tests/BaseApi.Tests/Processor/PipelineForwardFacts.cs` (D-08):
    1. DELETE `Assert.Equal(d.EntryId, inj.DeleteEntryId);` (`:149`). KEEP the `KeeperInject`
       capture (`:147`) and the `inj.Data` (`:148`) / `inj.EntryId` (`:150`) asserts.
    2. Adjust the NODROP-01 `<list>` doc comment (`:19`): remove `/DeleteEntryId` →
       "ONE `KeeperInject` carrying Data/EntryId" (Pitfall 6).

    B) `tests/BaseApi.Tests/Orchestrator/SC2RecoveryPathsE2ETests.cs` (D-09) — remove the
       WHOLE INJECT delete-half block (Pitfall 4 / A1 — NOT just the 3 cited lines; a literal
       3-line removal leaves a dangling `deleteEntryId`/`deleteKey` and a now-false delete
       assertion that will not compile / can never pass). Remove:
       - `var deleteEntryId = Guid.NewGuid();` (`:188`)
       - `var deleteKey = L2ProjectionKeys.ExecutionData(deleteEntryId);` (`:193`)
       - `await db.StringSetAsync(deleteKey, "source-to-delete");` (`:194`)
       - `factory.L2KeysToCleanup.Add(deleteKey);` (`:196`)
       - `DeleteEntryId = deleteEntryId,` (`:204`)
       - the `sourceDeleted` assertion `PollForKeyAbsentAsync(db, deleteKey, ct)` + its
         `Assert.True(sourceDeleted, …)` (`:212-214`)
       KEEP: the write assertion `PollForKeyValueAsync(db, entryKey, "inject-payload", ct)`
       (`:209-211`) and `factory.L2KeysToCleanup.Add(entryKey)` (`:195`).
       Update the doc comments (`:40-43`, `:178-179`): "write …, send …, delete
       `m.DeleteEntryId`. Asserted on … the source key being deleted." → "writes the data key
       + sends `StepCompleted`, deletes nothing. Asserted on the data key being written."
       Note: SC2 is `[Trait("Category","RealStack")]` — it must COMPILE but is NOT run in the
       hermetic gate (excluded by `Category!=RealStack`); live proof is operator-gated.

    C) Build gate (D-10): after the edits, run the full hermetic suite and the dual-config
       0-warning build. Both Release and Debug must report 0 warnings, full suite green.
  </action>
  <verify>
    <automated>dotnet test tests/BaseApi.Tests -- --filter-method "*PipelineForward*"; dotnet build -c Release; dotnet build -c Debug; dotnet test tests/BaseApi.Tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "DeleteEntryId" tests/` returns ZERO hits (all four test edits + the new fact are clean).
    - `grep -rn "DeleteEntryId" src/ tests/` returns ZERO hits across the whole repo code tree.
    - `SC2RecoveryPathsE2ETests.cs` contains NO `deleteKey`, NO `deleteEntryId`, and NO `PollForKeyAbsentAsync(db, deleteKey` reference; still contains `PollForKeyValueAsync(db, entryKey` (the kept write assertion); the file compiles.
    - `PipelineForwardFacts.cs` contains NO `inj.DeleteEntryId`; the NODROP-01 doc no longer says `/DeleteEntryId`.
    - `dotnet build -c Release` reports 0 warnings AND `dotnet build -c Debug` reports 0 warnings (D-10).
    - `dotnet test tests/BaseApi.Tests` exits 0 (full hermetic suite green; SC2 RealStack excluded).
  </acceptance_criteria>
  <done>PipelineForwardFacts and SC2 E2E carry the reduced shape (E2E compiles, deletes nothing); repo-wide DeleteEntryId grep is empty; full suite green; 0-warning Release + Debug build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → keeper consumers | Tests construct real consumers against a `RecoveryTestKit.Db()` substitute. No production trust boundary is added or removed — this plan only edits test code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-03 | Tampering (regression — destructive op silently re-introduced) | InjectConsumer / ReinjectConsumer delete invariant | mitigate | Behavioral negative-guard fact (`KeeperDeleteInvariantFacts`) runs the REAL `HandleAsync` and asserts `DidNotReceive().KeyDeleteAsync(...)` on BOTH overloads (single-key + `RedisKey[]`), co-asserted with a positive side-effect so a no-op consumer cannot pass — a re-introduced delete at runtime fails the build (KINJ-03). |
| T-70-04 | Tampering (vestigial field silently re-added) | KeeperInject contract | mitigate | Reflection `Assert.Null(typeof(KeeperInject).GetProperty("DeleteEntryId"))` in `KeeperContractTests` — re-adding the field fails the test AND every producer/consumer reference fails to compile (KINJ-02). |
| T-70-05 | (carve-out — false invariant) | L2ProbeRecovery scratch self-delete | accept | The invariant fact is behavioral and never instantiates `L2ProbeRecovery`, so its `:35` scratch delete is structurally outside the assertion — no IL/type scan that would wrongly flag it (this is why D-04 chose behavioral). |

**Conclusion:** No high-severity threats. This plan adds test-only regression guards that
STRENGTHEN the security posture (a re-introduced destructive op or a re-added field becomes
a build failure). The L2ProbeRecovery carve-out is handled structurally, not by suppressing
a real threat. ASVS V2/V3/V4/V5/V6 surfaces unchanged (70-RESEARCH.md §Security Domain).
</threat_model>

<verification>
- `dotnet test tests/BaseApi.Tests -- --filter-method "*KeeperDeleteInvariant*"` → 3 facts pass.
- `dotnet test tests/BaseApi.Tests -- --filter-method "*InjectConsumerFacts*|*KeeperContractTests*|*PipelineForward*"` → green.
- `grep -rn "DeleteEntryId" src/ tests/` → ZERO hits.
- `dotnet build -c Release` → 0 warnings; `dotnet build -c Debug` → 0 warnings (D-10).
- `dotnet test tests/BaseApi.Tests` → full hermetic suite green (SC2 RealStack excluded by `Category!=RealStack`; it compiles).
</verification>

<success_criteria>
- The KINJ-03 invariant fact proves DELETE deletes (multi-key) and INJECT + REINJECT do not (both overloads, positive co-assertion).
- KeeperContractTests reflection guard makes re-adding DeleteEntryId a build break (KINJ-02).
- InjectConsumerFacts, PipelineForwardFacts, SC2 E2E reshaped to the reduced id-set and green/compiling (KINJ-02).
- Repo-wide `DeleteEntryId` grep empty; full suite green; 0-warning Release + Debug (KINJ-02 / D-10).
</success_criteria>

<output>
After completion, create `.planning/phases/70-processor-inject-cleanup/70-02-SUMMARY.md`.
</output>
